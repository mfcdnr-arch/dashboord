# Генератор документации Dashboard (.docx с реальными скриншотами)

Пересобирает 5 документов в `docs/`: Паспорт системы, Установка и настройка,
Руководства администратора / модератора / пользователя.

## Предпосылки
- Запущенный dev-стек (`docker compose -p dashbord up -d`) и dev-фронт на :3080
  (vite, прокси на :8080); учётка `admin/admin`.
- Node 18+. Зависимости (один раз, в этой папке):
  `npm install docx playwright docx-preview jszip --no-save && npx playwright install chromium`

## Порядок
0. Разделы, вышедшие после 31.07.2026 (витрины, обращения, кабинет, папки,
   аномалии, подсказки виджетов/метрик, ретенция в UI), снимаются отдельным
   скриптом — им нужны свои временные данные:
   `python3 prepare_data.py && node shoot_sections.js` (в конце — `python3 prepare_data.py --cleanup`
   и SQL-подчистка, который он печатает). Кадры кладутся в тот же `shots/`.
   Скрипт `append_sections.py` нужен ТОЛЬКО чтобы дописать эти главы в уже
   собранные .docx без полной пересборки; при обычном прогоне он не нужен —
   главы уже описаны в `doc_*.js`.
1. `node shoot_all.js` — снимет базовые ~48 скриншотов в `shots/` (сам создаёт и
   удаляет временные учётки ztest_mod/ztest_user и временный архив-слепок;
   в конце БД чистая — скрипт печатает ALL DONE).
1b. `node shoot_v2.js` — метрики, список и просмотр дашборда, «О дашборде»,
   режим раскладки, объекты, выпуски, меню зрителя (снимаются на РЕАЛЬНЫХ
   данных стенда, свои временные данные не нужны).
1c. `node shoot_v3.js` — разделы 12–17.08: фильтр периода (в том числе состояние
   «за период отчётов нет»), меню «куда дальше», «⚑ проблема», аналитика папки,
   «Руководителю».
1e. `node shoot_v6.js` — досъёмка 27.08–11.09: раздел «Статистика услуг ДНР»,
   «Быстрый доступ», список узнаваемых форм на «Загрузке», ступени формы,
   полоса лестницы и страница «Разбор по ступеням», пять новых типов виджетов,
   матрица по месяцам, водопад по периодам, тепловая карта, честная обрезка
   «Сравнения», подсказка мини-графика. Снимается на РЕАЛЬНЫХ данных стенда,
   временных данных не создаёт.
1d. `node shoot_v4.js` — два кадра обратной связи: карточка обращения с
   контекстом и запрос доступа глазами зрителя. Скрипт САМ заводит временное
   обращение и САМ его убирает; нужна учётка `viewer1` с доступом хотя бы к
   одному дашборду.
2. `node doc_user.js && node doc_moderator.js && node doc_admin.js && node doc_install.js && node doc_passport.js`
   → готовые .docx в `out/`.
3. Проверка рендера: `node preview.js out/<файл>.docx имя` → PNG-страницы в `out/`.
4. Скопировать .docx в `../../docs/`, закоммитить, пересобрать архивы поставки
   (`git archive --prefix=Dashbord/ -o Final_v.1.tar HEAD`, аналогично zip).

Правило проекта: документация обновляется в день изменения функционала.

## Тех-уроки (зашиты в скрипты — не ломать)
- SQL в psql передаётся через **stdin** (bcrypt-хэш содержит `$`, argv через shell его портит).
- Клик по строкам списков — `page.evaluate` по «самому глубокому элементу,
  содержащему текст»: названия — текст-ноды среди span'ов, точного leaf нет.
- Возврат из открытого дашборда к списку — **хлебной крошкой**, nav-кнопка
  раздела состояние не сбрасывает.
- `img('имя')` падает с ENOENT, если кадра нет: ссылаться в главах только на
  реально снятые кадры (или фильтровать через `fs.existsSync`, как сделано для
  шагов мастера).
- Ответ `GET /dashboard-pages/{id}/widgets` — ОБЪЕКТ `{page_id, widgets:[…]}`,
  а не массив.
- В doclib children проходят `.flat(Infinity)` — незаспредленный массив иначе
  даёт битый XML (`<0/>`).
- 🔴 «Возврат хлебной крошкой» (см. выше) с 22.08 работает НЕ на тексте
  `'Дашборды'` — крошка открытого дашборда называется **«← Дашборды»** (со
  стрелкой). Старый `backToList` в `shoot_v3.js`/`shoot_v4.js` ищет точное
  совпадение `'Дашборды'` и попадает в пункт БОКОВОГО МЕНЮ вместо крошки —
  а он состояние открытого дашборда внутри `DashboardsPage` не сбрасывает
  (сбрасывает только `openDash` в `App`, не внутренний `sel` страницы,
  смонтированной с пустыми deps). Следующий шаг сценария молча снимает кадр с
  ПРЕЖНЕГО дашборда. Верный фильтр: `/^←\s*Дашборды$/`. В `shoot_v3.js`/
  `shoot_v4.js` не переносилось — работает лишь потому, что там `backToList`
  вызывается один раз в самом конце сценария.
- `PassportDialog` (паспорт цифры) НЕ слушает `Escape` — закрывается только
  кликом по backdrop или по кнопке ✕. `page.keyboard.press('Escape')` молча
  ничего не делает, диалог остаётся смонтированным (портал в `body`) и
  засвечивает ВСЕ следующие кадры до первой полной смены страницы. Закрывать
  явным кликом по ✕ внутри самого верхнего `position:fixed`-блока
  (см. `closeTopModal` в `shoot_v5.js`) — и не полагаться на Escape нигде,
  пока не проверено для конкретного диалога.
- Учётка `viewer1` — сбросить пароль до `Viewer1pass!` через `POST
  /users/{id}/reset-password` и снять `must_change_password` (`update users
  set must_change_password=false where login='viewer1'`), если вход отваливается
  с 401/403 — пароль на дев-стенде теряется между сессиями чаще, чем хотелось
  бы. Это служебная докген-учётка, трогать реальные учётки заказчика так
  нельзя.
- `bash`: имя `UID` — **зарезервированная переменная** (numeric user id),
  присвоение ей молча ломает следующие арифметические выражения оболочки с
  непрозрачной ошибкой. Для id сущностей использовать любое другое имя.
- 🔴 **Крупные .docx не уходят в репозиторий с этой машины.** Пакет больше
  ~10 МБ падает с `RPC failed; HTTP 408` — причём «Writing objects: 100 %»
  проходит на полной скорости, обрывается ОТВЕТ сервера. Что помогает:
  `git push --no-thin` (сервер не пересобирает дельты крупных двоичных
  файлов) и по одному файлу на коммит. Что НЕ помогает: `http.postBuffer`
  любой величины, `http.version=HTTP/1.1`, повторы (проверено 25 подряд).
  Отправка файлов в мобильный вид отваливается на тех же размерах — то есть
  дело в канале, а не в git. Закладывать на выкладку документации время.
- Кадры снимаются при `deviceScaleFactor: 2`, а в документе картинка всё
  равно ставится на ширину текста (612pt). После съёмки имеет смысл ужать:
  `for f in shots/*.png; do sips -Z 1600 "$f"; done` — файлы легчают на 15 %
  без потери читаемости (1600px на 8,5 дюйма ≈ 190 dpi).
- `GET /dashboards` отдаёт ОБЪЕКТ `{total, limit, offset, items}` — не массив;
  `.items` обязателен, иначе `dashboards.find is not a function`.
- Блок «На что посмотреть» рисуется ТОЛЬКО при наличии замечаний. На ежедневном
  отчёте РЦО их нет, и кадр снимать надо на «окнах и часах» (там их два).
- Поиск блока «самым глубоким элементом с текстом» не работает, когда строка
  разбита разметкой: «Ниже — уровень <b>Ведомство</b>» листом не является.
  Искать по отдельно стоящему значку (🪜) и подниматься к предку нужной высоты.
- Высокий виджет (показатели списком, матрица, строки с мини-графиками) в
  документе растягивается на ширину текста: кадр 1:2,8 занял бы две с половиной
  страницы. `shotWidget` в `shoot_v6.js` режет по соотношению 0,8 — и ПЕРЕД
  обрезкой ставит верх карточки к верху окна, иначе клип выходит за кадр
  («Clipped area is either empty or outside the resulting image»).
- После `scrollIntoView({block:'start'})` верх карточки накрывает липкая шапка
  страницы (вкладки + строка контекста) — нужен `window.scrollBy(0, -130)`,
  иначе в кадр попадает заголовок чужого блока.
- `!` в пароле внутри одинарных кавычек всё равно триггерит history expansion
  интерактивного bash/zsh — `set +H` или пароль без `!` в одноразовых
  командах.
