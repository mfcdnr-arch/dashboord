# Зависимости только для тестов (в прод-образ НЕ попадают).
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1

# Статический анализ (страховка перед деплоем на реальное железо) — CI job "lint".
ruff==0.16.1
mypy==1.19.1
