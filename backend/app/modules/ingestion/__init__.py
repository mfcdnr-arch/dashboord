"""Модуль «Ingestion» — конвейер извлечения данных из документов.

Поток: документ (MinIO) → парсер по формату → распознавание таблиц/шапок/типов
→ предпросмотр и ручная правка → выпуск датасета (dataset_release).

Подмодули:
- parsers.py   — форматные парсеры (xlsx/xls/csv/docx/pdf) → нормализованные таблицы;
- analyze.py   — определение строк-шапок и типов столбцов (чистые функции);
- service.py   — оркестрация задания извлечения + запись в БД;
- worker.py    — фоновый воркер (arq/Redis);
- router.py    — HTTP-эндпоинты.
"""
