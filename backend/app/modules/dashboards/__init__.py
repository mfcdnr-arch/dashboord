"""Модуль «Дашборды» (этап 5).

Модель: дашборд → именованные страницы (dashboard_pages) → виджеты (widgets на сетке).
Виджет берёт данные из метрики (KPI/план-факт) или из датасета (таблица/график)
и отдаёт готовый ряд для отрисовки на фронте (ECharts).

Подмодули:
- service.py — CRUD дашбордов/страниц/виджетов + вычисление данных виджета;
- router.py  — HTTP-эндпоинты.
"""
