-- 019: расширенная информация по показателю (FR-5.9).
-- Необязательный текст, который заполняет модератор; показывается при
-- «провале» в показатель (drill). Если пусто — UI покажет «Информации нет,
-- в разработке».
alter table metrics add column if not exists info_text text;
