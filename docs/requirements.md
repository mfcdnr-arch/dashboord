# Dashboard — Требования и решения (живой документ)

> Здесь фиксируем договорённости по мере обсуждения. Обновляется в ходе проекта.
> Роль/промт архитектора — в `/Users/denis/Dashbord/CLAUDE.md`.

Дата старта: 2026-07-10.

---

## 1. Общая идея

Корпоративная система дашбордов с админ-панелью:
- **Модератор** собирает информацию: часть вносит руками, часть тянется автоматически.
- Данные загружаются и **модерируются** через панель управления (черновик → на проверке → опубликовано).
- **RLS**: каждый пользователь видит только то, что разрешил администратор.
- **Дашборд рендерится в браузере (HTML)**, интерактивный.
- **Декомпозиция**: каждое значение раскрывается (формула, источник, владелец, свежесть, достоверность, детализация до первички).

---

## 2. Подтверждённые решения

- [x] **Модульная архитектура кода — ОБЯЗАТЕЛЬНО.** Не один большой файл. Делим на изолированные модули/сервисы: править легче, ошибки искать легче, каждую часть проверяем на работоспособность по ходу написания. (см. раздел 4).
- [x] **Архив / временной контур.** Вкладка «Архив»: просмотр данных за любой промежуток в пределах года. Глубина хранения — **1 год** (скользящее окно 12 мес.). Два механизма: time-series (ряд значений) + as-of снапшоты («как было на дату»). Партиционирование по месяцам, предагрегаты, фоновая очистка/архивация старше года.
  - [x] Решено: **жёсткое удаление** данных старше года (без холодной копии).
- [x] **Прозрачность KPI** — killer-фича: клик по любому числу раскрывает структуру.
- [x] **Модерация перед эфиром** — данные в дашборд попадают только после approve.

## 2a. ВАЖНО: в папке уже есть проработанный дизайн (обнаружено 2026-07-11)

Предыдущая сессия оставила реальные артефакты (не черновики), которые надо использовать как основу:
- **SQL-схемы (PostgreSQL 16):** `widget_permissions_full.sql` (orgs/users/roles/folders/dashboards/widgets/versions + ACL-слой securable_objects/object_acl + рекурсивная `fn_resolve_access` + кэш + уведомления + аудит-триггеры), `lifecycle_schema.sql` (documents→extraction→dataset_releases→metrics→widget_versions→publication), `rbac_visibility_schema_v2.sql` (access_grants dashboard/widget), `abac_dataset_release_fields.sql` (ABAC-теги), `moderation_state_machine.sql` (полный жизненный цикл + reason codes + transition rules + SLA + вторая проверка), `lineage_query.sql`.
- **Формулы:** `formula_dsl_grammar.ebnf` (DSL: SUM/AVG/RUNNING_TOTAL/PERIOD_COMPARE/PLAN_FACT/SHARE_OF_TOTAL), `cycle_detection.py` (детектор циклов + topo-sort).
- **Диаграммы:** `er_diagram.mmd`, `lifecycle_er_diagram.mmd`.
- **Модуль доступа:** `access-control-module/*.ts` — **на NestJS** (портируем на Python).
- **docx:** «Идея Дашборда» (авторское видение), «Реестр архитектурных решений», «ответы_проектирование», опросник.
- **Дубликат папки:** `/Users/denis/Dashbord Pref` — копия/бэкап, НЕ трогаем. Рабочая — `/Users/denis/Dashbord`.
- «Архив из 26 документов» (упомянут в docx) на диск НЕ распакован — физически его нет.

## 2b. Согласованные решения (2026-07-11, через опросник)

- **Стек бэкенда:** **Python + FastAPI** (модуль доступа с NestJS портируем на Python; SQL-схемы переносимы как есть).
- **Домен:** **универсальная платформа**, МФЦ ДНР — первое внедрение (движок generic: объекты→папки→файлы→дашборды).
- **Права:** **тонкие — папка→дашборд→виджет + по конкретному пользователю** (модель securable_objects/object_acl + access_grants уже есть). Row-level (по подразделению, ABAC) — отложено до появления подразделений.
- **Ввод данных:** **полный конвейер с авто-распознаванием** таблиц/столбцов/значений (documents→extraction→dataset_releases). Внешние авто-коннекторы (БД/API) — позже.
- **Ретенция:** удаление данных старше 1 года (совместимо со статусом `archived`).
- **Масштаб (из «ответов»):** on-premise, до ~20 пользователей на старте, 30–100 KPI, приоритет — полнота функционала, не скорость MVP.
- **Единицы/валюты:** несколько единиц измерения, валюта — рубль (уточнить при необходимости).

## 2c. Уточнено 2026-07-12: карточка пользователя и аудит входов (модули «Пользователи»/«Отчёты»)

Требования зафиксированы, реализация — по плану (после этапа 4), отдельным этапом «Пользователи + аудит входов».

- **Карточка пользователя при заведении:** отдельные поля **Фамилия / Имя / Отчество** (не одно `full_name`), **Отдел**, а также логин, email, роли, активность. `full_name` оставляем как отображаемое/вычисляемое (ФИО).
- **Отдел — СПРАВОЧНИК:** новая таблица `departments` (отделы организации), у пользователя — выбор из списка (`users.department_id`). Даёт переиспользование, фильтрацию отчётов по отделу и задел на будущий row-level доступ по подразделению (ранее отложенный).
- **Заведение пользователей:** CRUD-модуль `admin`/`users` (создать/редактировать/деактивировать; назначить роли; выдать временный пароль с `must_change_password`). Управляет `admin`.
- **Аудит входов:** фиксировать событие входа — отдельная таблица `login_events` (`user_id`, `ip_address`, `user_agent`, `success`, `created_at`); запись в эндпоинте `/auth/login`.
- **Отчёт по пользователям (FR-8.8–8.10):** «кто / когда / откуда (IP) / сколько раз заходил» — агрегаты по `login_events` (число входов, последний вход, история; фильтры по периоду/отделу/пользователю). Часть **«куда заходил»** (какие дашборды смотрел, рейтинг просмотров) — логируется через `audit_log`/`view`, когда появится **Viewer** (этап 5+); для этого при появлении просмотров задействовать action `view`.

## 2d. Пожелания заказчика 12.07.2026 → детализация этапа 5 и «Главной»

Источник: `Пожелания 12.07.pages` (8 пунктов). Кратко и как учтено:

1. **Сравнение одинаковых таблиц за разные даты** + диаграммы изменений + динамика за произвольный период (выбор из календаря). → Ряд по периодам у нас уже есть (по одному `dataset_release` на период), поэтому включаем оконную `PERIOD_COMPARE` и фильтр-календарь в этапе 5 (виджет «динамика»).
2. Дашборды — удобочитаемые, информативные, эргономичные, лёгкие в поиске/просмотре. → Планка качества Viewer.
3. **«Главная»** с датой, временем, числом дашбордов, числом пользователей (+ доп. блоки).
4. Диаграммы разных видов (круговые, горизонт./вертик., кривые…) по типу данных. → Виджеты на ECharts + автоподбор типа.
5. Админ/модератор сами размещают, где и на какой странице диаграмма. → Конструктор: раскладка виджетов на сетке страницы.
6. Админ/модератор дают **названия страницам**. → Сущность «страница» с именем/описанием.
7. На «Главной» — **кликабельный каталог страниц** с описанием. → Блок каталога.
8. Пересмотреть проект, предложить улучшения. → Постоянная задача архитектора.

**Принятые решения (2026-07-12):**
- **Модель:** `дашборд → именованные страницы → виджеты`. Дашборд группирует тематические страницы; у страницы — имя, описание, сетка (grid) виджетов. Новая сущность **`dashboard_pages`** (dashboard_id, name, description, position, layout jsonb); размещение виджетов — на сетке страницы (пожелания 5–6).
- **«Главная» — ГИБРИД:** базовые блоки фиксированы, набор ключевых KPI на главной выбирает админ.
- **Блоки «Главной» (MVP, все четыре):** (а) обзор — дата/время/приветствие + счётчики (дашбордов/объектов/метрик/пользователей); (б) **каталог страниц** — кликабельный список с описанием (пожелание 7); (в) ключевые KPI (значение + динамика ↑↓ + спарклайн) + сработавшие KPI-алерты; (г) активность — лента «что нового», популярные/избранные, свежесть данных.
- Зависимость: «число пользователей» и «популярные по просмотрам» опираются на модуль «Пользователи + аудит входов» (§2c, отложен) — на «Главной» показываем доступное сейчас, эти показатели включаем после аудита.

## 3. Стек (финализирован)

- Backend: **FastAPI (Python)**, строго модульно (пакеты/роутеры/сервисы/тесты по доменам)
- Frontend: **React + TypeScript** (viewer + админка); графики — **Apache ECharts**
- БД: **PostgreSQL 16** — единственное хранилище (мета, права, метрики, данные, версии, архив)
- Кэш/очереди/воркеры: **Redis** (+ фоновые задачи: извлечение документов, пересчёт метрик, очистка архива)
- Файлы: **MinIO** (хранение загруженных документов)
- Всё в **Docker**, автоустановка на железо (как в проекте DS)

---

## 4. Принцип модульности (правило разработки)

Строим НЕ монолитным файлом, а как набор изолированных частей:

- Backend — по модулям/роутерам: `auth`, `access` (RBAC/RLS), `ingestion`, `moderation`,
  `metrics`, `dashboards`, `archive`, `admin`, `notifications`. Каждый — своя папка/роутер/сервис/тесты.
- Frontend — по фичам (feature-sliced): каждый экран/виджет — отдельный компонент.
- Каждый модуль **пишем и проверяем на работоспособность отдельно**, до перехода к следующему
  (smoke-проверка + мини-тест), чтобы ошибки ловились локально.
- Никаких «богов-файлов» на тысячи строк.

---

## 5. Открытые вопросы

См. `docs/questionnaire.md` — ответы определят финальную архитектуру.

---

## 6. Журнал решений

| Дата | Решение |
|------|---------|
| 2026-07-10 | Заведён проект, роль-промт в CLAUDE.md |
| 2026-07-10 | Добавлен архив (год, скользящее окно) |
| 2026-07-10 | Зафиксирован принцип модульной разработки; создан опросник |
| 2026-07-11 | Изучен готовый дизайн в папке; сняты 4 развилки (FastAPI / универсальная / тонкие права / полный конвейер) |
| 2026-07-11 | Создан комплект docx в docs/Проектирование/ (00–05); реестр открытых вопросов для Q&A |
| 2026-07-11 | Ввод данных РЕШЕНО: форматы Excel/PDF/CSV/Word; таблицы простые+сложные; ручная правка маппинга; отчётная дата вручную; OCR не нужен. Создан док 06 |
| 2026-07-11 | Добавлены требования: LAN, вывод в браузер, отчёты популярности + диаграмма просмотров (FR-8.8–8.10), LAN (FR-9.3) |
| 2026-07-11 | Поднят Docker-стек Dashbord (postgres:15@55432, redis@6380, minio@9800/9801) — всё healthy. Прод-цель PG16 |
| 2026-07-11 | ОВ-13 РЕШЕНО: фильтры (период/объект/категория/произв.столбцы), глобальные+виджетные (как Power BI), пресеты; отчёты в разрезах период/пользователь/дашборд/роль/папка; диаграмма во времени+топ+по ролям. Создан док 16 |
| 2026-07-11 | ОВ-07 РЕШЕНО: формулы визуально+текст; текущего DSL достаточно; межфайловые расчёты (значение из одной таблицы ⨯ значение из другой); авторы — админ+модератор; предпросмотр обязателен. Создан док 07. Ячейка = НАЗВАНИЕ СТРОКИ + столбец (не позиция) |
| 2026-07-11 | ОВ-08 РЕШЕНО: виджеты MVP (KPI/таблица/линия/столбцы/круговая/план-факт; карта/воронка/radar/heatmap — фаза 2); тултипы из метрики+override; drill 2 уровня (формула+первичные строки); шаблоны+с нуля; собирают админ+модератор. Создан док 08 |
| 2026-07-11 | ОВ-09 РЕШЕНО: одна ступень (модератор публикует сам), конфликт интересов (не одобряешь своё), короткий справочник причин, чек-лист 6 блоков, без SLA, версии+откат. Создан док 09 |
| 2026-07-11 | ОВ-10 РЕШЕНО: ~20 польз., 2–3 сек; ежедневный бэкап; АВТО-ТЕСТИРОВАНИЕ сервера (healthcheck+smoke+самодиагностика); ОС прод — Astra Linux Special Edition; LAN сейчас + задел на внешний доступ. Создан док 10 |
| 2026-07-11 | Уточнено: один сервер (одна машина) + десятки одновременных подключений; HA не требуется на старте |
| 2026-07-11 | ОВ-11 РЕШЕНО: две зоны (админка+viewer), левое меню+крошки, светлая/тёмная, десктоп-первый, русский. Бренд — из бренд-бука МФЦ (⏰ напомнить запросить). Создан док 11 |
| 2026-07-11 | Добавлено: разделы админки Бэкапы/Импорт-экспорт/Мониторинг сервера; полноэкранный режим; стартовый экран-витрина; ТРИ домена отчётности (бизнес / посещаемость / сервер-железо CPU-RAM-диск с порогами). FR-5.7–5.8, FR-8.11–8.15. Доки 02/11/16 |
| 2026-07-11 | ОВ-14 РЕШЕНО: MVP — экспорт/KPI-алерты/избранное/поиск/ротация; позже — бенчмарк/домашний дашборд/планировщик/комментарии/лента; ❌ ИИ отклонён. FR-10 |
| 2026-07-11 | Сквозное требование: админ+дашборды многофункциональные/информативные; drill в ЛЮБОЙ показатель → расширенная инфо (заполняет модератор, необязательно; если пусто — «Информации нет, в разработке»). FR-5.9, док 08 (drill уровень 3) |
| 2026-07-11 | Проект подключён к GitHub: https://github.com/denregeda/dashbord (приватный), main→origin/main, первый коммит (42 файла), .env исключён |
| 2026-07-11 | Этап 0 (БД): миграции 001-006 собраны и накатаны (42 таблицы, проверено), авто-накат + apply_migrations.sh. Ветка etap-0-karkas |
| 2026-07-11 | Добавлено: в панель — справочник УСЛУГ (перечень) + служебные/справочные документы для модерации (FR-8.16/8.17, доки 02/09/11) |
| 2026-07-11 | gh CLI установлен и авторизован (denregeda); ветка etap-0-karkas запушена; открыт PR #1 (миграции БД) |
| 2026-07-11 | Этап 0 (бэкенд): скелет FastAPI (модульный, app/modules/*), пул asyncpg, /health, /system/info; сервис api в compose (8080). Проверено локально: /health=ok, видит 42 таблицы. Запушено в PR #1 |
| 2026-07-11 | Уроки ВМ из DS зафиксированы в доке 10 (часы/apt, MinIO CPU-пин через MINIO_IMAGE, amd64, офлайн-образы) |
| 2026-07-11 | Этап 0 (фронтенд): Node LTS v24 установлен; скелет React+TS+Vite (App, две зоны, индикатор API), прокси на 8080. Проверено вживую: build OK, фронт+бэк+БД связаны (/health=ok через прокси). Запушено в PR #1. ЭТАП 0 ЗАВЕРШЁН |
| 2026-07-11 | PR #1 смёржен в main; создана ветка etap-1-auth |
| 2026-07-11 | Этап 1 (бэкенд auth): модуль auth (bcrypt+JWT, bootstrap org/роли/admin, /auth/login, /auth/me), миграция 007 (must_change_password). Проверено: login/me/401/сид 7 ролей. Ветка etap-1-auth. Осталось: экран входа во фронте |
| 2026-07-11 | Этап 1 (фронт+эндпоинт): /auth/change-password; экраны Login/ChangePassword, оркестрация App. Проверено ВЖИВУЮ в браузере (скриншоты): admin/admin→смена пароля→оболочка (API ok, роль admin). ЭТАП 1 ЗАВЕРШЁН |
| 2026-07-12 | Исправлено имя бренда Dashbord→Dashboard во всех надписях (экран входа «Dashboard ГБУ «МФЦ ДНР»»). PR #2 (этап 1) смёржен в main. Ветка etap-2-objects |
| 2026-07-12 | Этап 2.1 (бэкенд объекты): миграция 008 (objects + folders.object_id), модуль objects (CRUD объектов/папок), require_roles(admin,moderator). Проверено: создание/список/папки, 401/409. Прим.: pydantic-модели используют Optional[...] (локальный venv Python 3.9) |
| 2026-07-12 | Этап 2.2 (бэкенд документы): модуль documents (загрузка файла в MinIO + document_versions, отчётная дата вручную, форматы xlsx/xls/csv/pdf/docx), storage.py (MinIO client + бакет). Проверено: загрузка CSV (файл в бакете), список, .txt→400. Зависимость minio |
| 2026-07-12 | Этап 2.3 (фронтенд): оболочка с боковым меню; раздел «Объекты» (ObjectsPage): объект→папки→документы, создание объекта/папки, форма загрузки (файл+дата), хлебные крошки; vite-прокси +/objects,/folders. Проверено ВЖИВУЮ (скриншоты): создание объекта «МФЦ Донецк»→папка→форма документов. ЭТАП 2 ЗАВЕРШЁН |
| 2026-07-12 | Смена сессии/папки: `.claude/` → в .gitignore; фикс `backend/README.md` (локальный запуск требует MINIO_ENDPOINT=localhost:9800). Health-check: PG (43 табл., миграции 001–008) + Redis + MinIO healthy, backend `/health`=db:ok, фронт `tsc`/`vite build` чисто. Коммит c23368c |
| 2026-07-12 | Этап 3 (старт) — сняты 3 под-вопроса док-06: **канонические поля — локальные (на объект/папку)**; **дубль периода — спрашивать пользователя** (конфликт→выбор supersede/оставить); **извлечение — фоновый воркер на Redis (arq)**. Предпросмотр — первые 100 строк (дефолт). Найдено: под значения ячеек нет таблицы → будет миграция 009 |
| 2026-07-12 | Этап 3.1 (бэкенд, движок извлечения): модуль `ingestion` — parsers.py (xlsx/xls/csv/docx/pdf: объединённые ячейки, многоэтажные шапки, авто-кодировка/разделитель CSV, скан-PDF→warning), analyze.py (определение шапки + тип столбца number/date/text с уверенностью), service.py (job→парсинг→БД), воркер arq + очередь, router (POST extract, GET job/version). Миграция 009 (extracted_tables.data+header_rows, extraction_jobs.warnings). storage.get_object. Сервис `worker` в compose. Проверено СКВОЗНО (login→объект→папка→CSV→extract→воркер→succeeded, conf 1.0, типы верные, data в БД, статус doc=extracted). Осталось: 3.2 маппинг+выпуск датасета, 3.3 фронтенд |
| 2026-07-12 | Этап 3.2 (бэкенд, маппинг+выпуск): mapping.py — авто-предложение маппинга (транслит-код поля, тип, столбец-метка строки), выпуск `dataset_release` со статусом validated + материализация значений «в длину» (row_label + канон.поле → value_text/number/date). Миграция 010 (canonical_fields на ОБЪЕКТ, dataset_values, dataset_releases.object_id), миграция 011 (частичный unique-индекс: уникальность только среди активных выпусков — под supersede). Эндпоинты: mapping-suggestion, POST release, canonical-fields CRUD, list/get releases. Проверено СКВОЗНО: suggestion→release(9 знач.)→get; конфликт периода→409; supersede→новый validated+старый superseded; справочник полей заполнен. Осталось: 3.3 фронтенд распознавания |
| 2026-07-12 | Этап 3.3 (фронтенд, экран распознавания): компонент `ExtractionPage` — открывается кликом по документу; запуск/опрос распознавания, предпросмотр таблицы (подсветка шапки), редактор маппинга (вкл/выкл столбца, имя поля, тип, радио «метка строки»), форма выпуска (код/название/период), диалог конфликта supersede/отмена, панель успеха. `api.ts` +типы/функции ingestion; бэкенд: `version_id` в списке документов; vite-прокси +/document-versions,/extraction-jobs,/dataset-releases. Проверено ВЖИВУЮ в браузере (скриншоты): превью+авто-маппинг (метка=Услуга, типы верные), диалог конфликта «Услуги (v2)», выпуск (9 значений/3 строки + supersede), консоль чистая, tsc/build 0 ошибок. ЭТАП 3 ЗАВЕРШЁН. Дальше — этап 4 (метрики/формулы) |
| 2026-07-12 | Проверка запроса заказчика: заведение пользователя (Фамилия/Имя/Отчество + Отдел) и отчёт по входам (кто/когда/IP/сколько раз) СЕЙЧАС НЕ реализованы — это модули «Пользователи»/«Отчёты» (ещё не построены; `users` имеет только `full_name`, отделов нет, входы не логируются, в `audit_action` нет `login`). Решено: **отдел — справочник (таблица `departments`)**; реализовать **по плану, отдельным этапом «Пользователи + аудит входов» после этапа 4**. Требования зафиксированы в §2c |
| 2026-07-12 | Этап 4.1 (бэкенд, формульный движок — изолированно): модуль `metrics` — parser.py (токенизатор + рекурсивный спуск по formula_dsl_grammar.ebnf → AST-dict для jsonb; извлечение зависимостей datasets/metrics), evaluator.py (Resolver-протокол абстрагирует БД; ядро: арифметика, агрегаты SUM/AVG/COUNT/MIN/MAX+filter, field-скаляр, cell межпериодный, metric, PLAN_FACT_DELTA/PCT; оконные RUNNING_TOTAL/PERIOD_COMPARE/SHARE_OF_TOTAL парсятся, вычисление — позже), cycles.py (детектор циклов DFS + топосорт, порт cycle_detection.py). Тест `backend/tests/metrics_engine_smoke.py` — 35/35 зелёные. Не подключён к приложению/БД (это 4.2). Дальше: 4.2 модуль метрик (Resolver из dataset_values, CRUD+версии+зависимости+предпросмотр), 4.3 фронтенд-конструктор |
| 2026-07-12 | Этап 4.2 (бэкенд, метрики на данных): resolver.py — предзагрузка ссылок из AST + вычисление синхронным движком (field=столбец активного/последнего выпуска по коду; cell=выпуск за дату+row_label+код столбца; metric=рекурсивно с защитой от циклов; filter по row_label). service.py — CRUD метрик, версии формул (парс→AST→jsonb, зависимости в metric_dependencies, проверка циклов на графе кодов), статусы draft→validated→approved (свою не одобряешь, снятие прежней approved→deprecated), preview + вычисление версии. router.py: POST /metrics, версии, validate/approve, /versions/{id}/value, /metrics/preview. Фикс: cell-дата через `$3::text::date` (asyncpg). Подключено в main. Проверено СКВОЗНО (11/11): preview SUM/PLAN_FACT_PCT/cell/filter, деление на ноль→400, нет датасета→400, версия+deps+value=150, metric(ref)+10=160, validate, запрет одобрять своё→400, цикл A→B→A→400. Дальше: 4.3 фронтенд-конструктор метрик |
| 2026-07-12 | Этап 4.3 (фронтенд, конструктор метрик): раздел «Метрики» (MetricsPage) — список метрик (код/версии/ед./статус), создание метрики, детализация с версиями формул, редактор новой версии (textarea формулы + ед. изм.), ОБЯЗАТЕЛЬНЫЙ предпросмотр на реальных данных (значение + раскрытие зависимостей datasets/metrics), кнопка «Значение» (вычислить версию), статусы «Проверить»/«Одобрить», подсказка по формулам с кликабельными примерами. api.ts +типы/функции metrics; App: NAV «Метрики» ready + canManage; vite-прокси +/metrics. Проверено ВЖИВУЮ (скриншоты): список 4 метрик, value=150 шт, предпросмотр PLAN_FACT_PCT=96.67 (зависит от plan/fakt), запрет одобрять своё, создание v2 через UI, консоль чистая, tsc/build 0 ошибок. ЭТАП 4 (метрики) — бэкенд+фронт ГОТОВЫ (оконные функции RUNNING_TOTAL/PERIOD_COMPARE/SHARE_OF_TOTAL — вычисление отложено до временного контура). Дальше — этап 5 (виджеты/дашборды) |
| 2026-07-12 | Пожелания заказчика (`Пожелания 12.07.pages`, 8 п.) разобраны и зафиксированы в §2d как детализация этапа 5 + «Главная». Решения: модель `дашборд→именованные страницы→виджеты` (нов. сущность `dashboard_pages`); «Главная» — ГИБРИД (базовые блоки + выбор ключевых KPI админом); блоки MVP: обзор+счётчики, каталог страниц, KPI+алерты, активность/свежесть. Сравнение периодов (пож.1) — включаем `PERIOD_COMPARE`+календарь в этапе 5 (ряд по периодам уже есть). Реализация — этап 5 |
| 2026-07-12 | Функция процента `PERCENT_OF(база, значение)` = значение/база*100 (база=100%): парсер+вычислитель (защита база=0), смоук 38/38; в визуальном конструкторе — режим «Процент (доля)» (два операнда: база=100% и значение); инструкция и справочник в UI дополнены. Уточнено: в конструкторе датасет (документ) выбирается по коду/названию выпуска (в выпадашке теперь и объект); разные документы — разными кодами, либо `cell(..., date=)` по дате. Проверено вживую: собрал PERCENT_OF мышью→предпросмотр 100%, консоль чистая (пофикшено смешение border/borderColor). tsc/build 0 |
| 2026-07-12 | Продвинутое формирование дашбордов (по запросу заказчика, всё проверено): ① авто-сборка дашборда из объекта (`POST /dashboards/auto` — KPI/график/динамика/таблица по датасетам, rule-based); ② шаблоны (миграция 014 `dashboard_templates`; сохранить/создать из шаблона = клонирование); ③ drag-drop сетка (react-grid-layout, режим «Раскладка», PATCH позиции/размера); ④ мультисерийные виджеты (тип `compare` — неск. полей на графике); ⑤ интерактив cross-filter (param `row` в данных виджета, клик по категории→фильтр страницы); ⑥ версии+публикация (снимок в `dashboard_versions`, publish/unpublish/versions/restore-откат); экспорт страницы в PDF (html2canvas+jsPDF, клиентский, пагинация A4). Виджеты: kpi/table/bar/line/pie/plan_fact/dynamics/compare. Прокси vite +/dashboard-templates. ЭТАП 5 (дашборды) существенно расширен. Осталось: движок KPI-алертов |
| 2026-07-12 | Сравнение периодов (пожелание 1): виджет `dynamics` — ряд по периодам (для каждого активного выпуска датасета сумма поля) + Δ и Δ% к пред. периоду; эндпоинт `/widgets/{id}/data?from=&to=` (фильтр периода). Фронт: WidgetView рендерит динамику линией ECharts + «К пред. периоду ↑…»; в конструкторе тип «Динамика (периоды)»; на странице — фильтр-период (from/to, календарь), прокидывается во все виджеты. Проверено СКВОЗНО (9/9: 3 выпуска nagruzka 100/120/140 → периоды/значения/Δ=20(16.67%)/фильтр from) и ВЖИВУЮ (линия по месяцам, фильтр режет ряд). Ряд по периодам берётся из нескольких активных dataset_releases одного кода (partial unique index допускает). Осталось по этапу 5: движок KPI-алертов |
| 2026-07-19 | Отчёты — доп. блоки: КАЧЕСТВО ДАННЫХ + БИЗНЕС-СВОДКА (на живых данных, без инструментирования). Бэкенд (reports/service): `GET /reports/data-quality` — свежесть по объектам (число датасетов, последний период/выпуск, статус «нет данных»), список объектов без данных, ошибки расчёта метрик (evaluate_ast лучшей версии, ловим FormulaError); `GET /reports/business` — метрики с текущими значениями/ед/ошибкой + сработавшие KPI-алерты (переиспользован dashboards.list_org_alerts, admin=привилегированный видит все). Фронт ReportsPage +2 блока (таблица свежести, ошибки метрик, объекты без данных; показатели+алерты). Проверено СКВОЗНО (data-quality: Тест 3.1 нет данных, метрика cyc_a ошибка 1/3; business: Итого план 150 шт/План+10 106.67/A ошибка, алертов 0) и ВЖИВУЮ (оба блока, честно подсвечены пробелы данных; консоль чистая, tsc/build 0). Тестовые события очищены. NB: «Аудит действий» НЕ сделан — audit_log существует (49 старых записей от прототипа), но текущий код туда НЕ пишет; нужен кросс-каттинг helper _audit на ключевых мутациях (отдельный шаг). ДАЛЬШЕ: аудит действий (инструментирование) ИЛИ модерация дашбордов (последний пункт волны B). |
| 2026-07-19 | Волна B №3 (начало) — РАЗДЕЛ «ОТЧЁТЫ» + СИСТЕМНЫЙ МОНИТОРИНГ (Q5, акцент заказчика). Новый модуль `reports` (admin-only) + `psutil==6.1.1`. Бэкенд: `GET /reports/system` — CPU/RAM/диск (psutil, уровни good/warn/danger по порогам), load, ядра, аптайм, размер БД, статусы сервисов PG (select 1)/Redis (ping)/MinIO (bucket_exists); `GET /reports/attendance` — по login_events за 30 дн: входов/неудач/активных, по дням (14 дн), топ-5. main.py +reports_router; vite-прокси +/reports. Фронт: `ReportsPage` (nav «Отчёты» ready, admin-only) — гейджи CPU/RAM/Диск с цветными полосами, сервис-чипы, аптайм/БД; посещаемость (счётчики + мини-гистограмма по дням + топ); авто-обновление 15с. Проверено сквозно+вживую (CPU/RAM/Disk good, сервисы PG/Redis/MinIO зелёные, посещаемость, авто-обновление; консоль чистая, tsc/build 0). Тестовые события очищены. On-prem/psutil, БЕЗ внешних SaaS. ДАЛЬШЕ по «Отчётам» (в разработке): аудит действий, качество данных, модерация, бизнес-сводки. Затем — модерация дашбордов (последний пункт волны B). |
| 2026-07-19 | Волна B №2 — АУДИТ ВХОДОВ (login_events). Миграция 017: таблица `login_events` (org, user_id, login, ip, user_agent, success, created_at, индексы). `/auth/login` теперь пишет событие на КАЖДУЮ попытку (успех и неуспех): IP (X-Forwarded-For -> client.host), user-agent; при неудаче user_id/org=null если логин не найден. Отчёт: `GET /login-events` (admin) -> {summary: по пользователям входов/неудач/последний вход; recent: последние 50 кто/когда/IP/успех}; в users/service+router; vite-прокси +/login-events. Фронт: UsersPage +раздел «Журнал входов (аудит)» — таблица по пользователям + лента последних событий (галочка/крест, логин, ФИО, IP, время). Проверено СКВОЗНО (успех admin->200 записан; неудача admin/nosuch->401 записаны success=false с IP; summary admin входов=2/неудач=1/последний; recent корректен) и ВЖИВУЮ (раздел отрисован; консоль чистая, tsc/build 0). Тестовые события очищены. ЗАПУШЕНО на GitHub (бэкап, ветка синхронна с origin). ДАЛЬШЕ по волне B: отчёты (раздел «Отчёты» + системный мониторинг psutil) -> модерация. |
| 2026-07-18 | Волна B №1 — МОДУЛЬ «ПОЛЬЗОВАТЕЛИ + ОТДЕЛЫ» (фундамент волны B; раньше пользователи заводились только через SQL). Миграция 016: таблица `departments` (org, name, unique) + колонки users `last_name/first_name/middle_name/department_id`. Бэкенд — новый модуль `users` (только admin): отделы (list/create/delete), роли (list для выбора), пользователи (list с ФИО/отдел/роли/статус; create с временным паролем+must_change_password; update ФИО/отдел/email/роли; блок/разблок is_active с гардом «нельзя себя»; reset-password). Жёсткого удаления НЕТ (только блокировка — сохраняем историю/аудит). Роутер /departments,/roles,/users(+/{id},/active,/reset-password); vite-прокси +/users,/departments,/roles; main.py +users_router. Фронт — `UsersPage` (справочник отделов чипами; таблица пользователей; модалка создания/редактирования: логин/пароль/ФИО/email/отдел-выпадашка/роли-чекбоксы; действия ✎/🔑/🔒); App nav «Пользователи» ready=true, admin-only. Проверено СКВОЗНО (создан отдел+viewer2 с ФИО «Иванов Иван Иванович»/отдел/роль; вход временным паролем; не-admin→403; самоблок→400; блок→вход 401; reset→200; дубликат логина→400) и ВЖИВУЮ (страница, справочник отделов, таблица, модалка создания с ролями-чекбоксами; консоль чистая, tsc/build 0). Тестовые данные удалены. ДАЛЬШЕ по волне B: аудит входов (login_events) → отчёты (вкл. системный мониторинг) → модерация. NB: раздел «Пользователи» в nav теперь ready, но для не-admin показывает «доступно только администратору» (эндпоинты 403) |
| 2026-07-18 | Ответы на вопросы заказчика перед волной B (зафиксировано). (Q1 доступ пользователей) прод-вход НЕ настроен: в compose есть api(8080)/worker/PG/Redis/MinIO, но НЕТ frontend-сервиса и reverse-proxy → нужен nginx/Caddy: статика `dist/` + прокси API на один адрес `http://dashboard.<домен-или-IP>` (viewer+админка на одном URL, разделы по ролям). (Q2/Q5 ОТЧЁТЫ — план, сейчас раздел «Отчёты» пуст): **(а) посещаемость/аудит** — login_events (кто/когда/IP/сколько раз), активные/неактивные, просмотры дашбордов, рейтинг популярности, диаграмма по времени/ролям/отделам; **(б) аудит действий** — audit_log (создание/публикация/изменение дашбордов/метрик/выпусков); **(в) данные/качество** — свежесть, устаревшие датасеты, пропущенные периоды, ошибки расчёта метрик; **(г) модерация** — очередь, время approve, отклонённые; **(д) бизнес-сводки** — KPI с отклонениями план/факт, сработавшие алерты за период; **(е) СИСТЕМНЫЙ МОНИТОРИНГ (Q5)** — CPU/RAM/диск/сеть хоста, статусы сервисов (API/PG/Redis/MinIO/worker healthcheck), место в БД/MinIO, длина очереди воркера, время ответа API, с ПОРОГАМИ и алертами (сбор psutil, on-prem, без внешних SaaS). (Q3 модульность) бэк модульный по доменам — хорошо; НО раздулись `dashboards/service.py`=1031, `DashboardsPage.tsx`=924, `api.ts`=589 → отдельный тех-рефакторинг (разбить на подмодули/компоненты). (Q4) правило автоматизации деплоя — см. [[feedback_deploy_automation]], нужен «этап Деплой». (Q7 готовность деплоя) ~50-60% из коробки (ядро поднимется, фронт/единый вход/smoke/офлайн — доделать); после «этапа Деплой» → 90%+. (Q8 удобство зрителя) ~7/10 (нет чистого read-only Viewer, поиска/избранного, расширенной карточки показателя, моб.адаптива). (Q10 коннекторы) on-prem/LAN → внешние SaaS (Datadog/Slack/Notion/Jira/Figma…) НЕ подходят; для нашей разработки — GitHub; для прод-мониторинга — self-hosted Prometheus+Grafana+Loki в LAN, не коннекторы. Итог: волна B (админ/модерация/аудит/отчёты) актуальна; добавлены «этап Деплой» и тех-рефакторинг больших файлов |
| 2026-07-18 | Волна A (графичность конструктора, ревизия A5) — ФОРМУЛА ПРЯМО ИЗ ВИДЖЕТА. **ВОЛНА A ЗАКРЫТА.** KPI-виджет может нести собственную формулу (`config.formula` + `unit`), движок метрик считает её на лету БЕЗ создания метрики. Бэкенд: +`parse` в импорт, хелпер `_formula_value` (parse→evaluate_ast), KPI-ветка `_compute_widget` поддерживает `cfg.formula` (приоритет: формула→metric_code→dataset+field); `widget_drill` показывает формулу виджета как показатель уровня 1 + её датасеты (прозрачность). Фронт: WidgetForm — источник KPI +«Формула» (option только для kpi; сброс на metric при смене типа); поле единицы + текстовое поле формулы (правка/показ) + встроенный визуальный `FormulaBuilder` (onFormula→setFormulaDsl); живой предпросмотр через `/widgets/preview` (уже поддерживает formula). Проверено СКВОЗНО (preview `SUM(field('plan','kol'))+10`→160 шт; `metric('total_plan',version=latest)*2`→193.33; ошибка формулы→404; существующий KPI цел=140) и ВЖИВУЮ (Источник=Формула → поле+конструктор+единица, ввёл формулу → предпросмотр 160; консоль чистая, tsc/build 0). **ВОЛНА A (графичность конструктора) ПОЛНОСТЬЮ ЗАКРЫТА:** живой предпросмотр ✅ + подсказки «что собрать» ✅ + каталог источников ✅ + подсказки метрик ✅ + формула из виджета ✅. ДАЛЬШЕ — волна B (админ/модерация/аудит) |
| 2026-07-18 | Волна A (графичность конструктора, ревизия A4) — ПОДСКАЗКИ МЕТРИК: в выпадашке метрик видны единица и формула, чтобы нетехнический пользователь выбирал осознанно. Бэкенд: `list_data_sources` (metrics/service.py) теперь отдаёт по каждой метрике `unit` и `formula` лучшей версии (подзапросы с приоритетом approved→validated→draft). Фронт: api.ts +тип `MetricSource {code,name,unit?,formula?}`; WidgetForm — в опциях метрики подпись «name · ед»; под выпадашкой строка-подсказка «<метрика> · ед.: X · формула: <DSL>» (для plan_fact — отдельно План/Факт). Проверено СКВОЗНО (data-sources: Итого план ед.=шт формула=SUM(field('plan','kol')); План+10 формула metric('total_plan',version=latest)+10) и ВЖИВУЮ (выбрал «Итого план» → «· шт» в списке + строка формулы под ним + предпросмотр 150 шт; консоль чистая, tsc/build 0). ДАЛЬШЕ по волне A — последнее: формула прямо из виджета (мини-конструктор при добавлении KPI) |
| 2026-07-18 | Волна A (графичность конструктора, ревизия A2) — КАТАЛОГ ИСТОЧНИКОВ рядом с формой сборки (что можно применить). Только фронт (данные из `/metrics/data-sources`, бэкенд не менялся). Компонент `SourceCatalog` в блоке «Добавить виджет»: сворачиваемый (кнопка «📚 Каталог источников»); Датасеты — список с РОДОСЛОВНОЙ «📄 документ · папка · объект», клик раскрывает Поля/столбцы (имя + тип число/текст/дата + метка «строка»), Строки (значения row_label), Периоды (даты выпусков); Показатели (метрики) — чипы имя(код). Отвечает на требование «система показывает, какие документы/поля/столбцы/строки/показатели/формулы можно применить». Проверено ВЖИВУЮ: раскрыл nagruzka → Принято·число, Услуги·текст·строка / Строки Паспорт / Периоды 3 даты / родословная n.csv·Нагрузка·Динамика тест; метрики A/B/Итого план/План10; консоль чистая, tsc/build 0. ДАЛЬШЕ по волне A: подсказки метрик (единица/формула в tooltip), формула прямо из виджета |
| 2026-07-18 | Волна A (графичность конструктора, ревизия A3) — ПОДСКАЗКИ «ЧТО СОБРАТЬ»: система сама предлагает готовые виджеты под выбранный датасет, пользователь отмечает галочками и добавляет пакетом. Бэкенд: `suggest_widgets(conn, org_id, dataset_code)` — по числовым полям датасета строит спеки (KPI Σ на каждое поле; график по строкам; динамика — если >1 периода; сравнение+план-факт — если ≥2 полей; таблица-первичка), эндпоинт `GET /widgets/suggestions?dataset_code=...` (manage). Фронт: api.ts +widgetSuggestions/WidgetSpec; компонент `SuggestPanel` (кнопка «✨ Предложить виджеты под датасет» → выбор датасета → чипы-предложения с чекбоксами, все отмечены → «＋ Добавить выбранные (N)»); DashboardsPage `addWidgetsBatch` (цикл createWidget + reload). Проверено СКВОЗНО (nagruzka многопериодный → KPI/график/динамика/таблица; plan 1 поле → без динамики/сравнения; s32 2 поля → 2×KPI+график+сравнение+план-факт+таблица=6) и ВЖИВУЮ (панель, 6 чипов с бейджами, пакетное добавление 4→10 виджетов, ровно предложенные; консоль чистая, tsc/build 0). Тестовые виджеты удалены (10→4). ДАЛЬШЕ по волне A: каталог источников рядом с формой, подсказки метрик (единица/формула), формула из виджета |
| 2026-07-18 | Волна A (графичность конструктора, ревизия A1) — ЖИВОЙ ПРЕДПРОСМОТР ВИДЖЕТА при сборке/редактировании (как в конструкторе формул: видишь виджет ещё до добавления). Бэкенд: расчёт данных разделён — `compute_widget_data` (по widget_id, RLS) теперь тонкая обёртка над переиспользуемым ядром `_compute_widget(conn, org_id, t, name, cfg, from,to,row)`; +`preview_widget(...)` и эндпоинт `POST /widgets/preview` (manage; тело {widget_type,name?,config} → те же данные, что и у готового виджета, без сохранения; коллизий маршрутов нет — POST на /widgets/preview). Фронт: api.ts +previewWidget; WidgetView +export `WidgetPreviewBody` (рендер тела по данным); WidgetForm — общий `currentConfig()` (для submit и предпросмотра; null=данных мало), useEffect с дебаунсом 350мс дёргает preview и рисует `WidgetPreviewBody` в панели «Предпросмотр» (внизу формы, в т.ч. в модалке редактирования). Проверено СКВОЗНО (preview kpi=140/bar/text OK, пустой kpi→400, существующие виджеты не сломаны=регресс ок) и ВЖИВУЮ (метрика→«150 шт» на лету; тип «Столбцы»→настоящий график ECharts из 3 столбцов; ошибка расчёта цикл-метрики показывается; консоль чистая, tsc/build 0). ДАЛЬШЕ по волне A: подсказки «что собрать», каталог источников, подсказки метрик (единица/формула), формула из виджета |
| 2026-07-18 | Бэклог §7 приоритет 2 — Экспорт Excel/PNG (в доп. к PDF). **ПРИОРИТЕТ 2 §7 ЗАКРЫТ.** Excel — на БЭКЕНДЕ (openpyxl уже в venv, надёжнее+уважает RLS): эндпоинт `GET /dashboard-pages/{page_id}/export.xlsx` (Response, media_type xlsx); `export_page_xlsx` — лист «Сводка» (KPI/план-факт), по листу на датасетный виджет (table→колонки/строки, bar/line/pie→категория/значение, dynamics→период/значение, compare→категории+серии), text/image пропускаются; имена листов санитайзятся (без []:*?/\, ≤28 симв, дедуп); RLS через `_can_view`. PNG — КЛИЕНТСКИЙ (html2canvas→canvas.toDataURL('image/png')→download, как PDF, снимок страницы). api.ts +exportPageXlsx (blob). DashboardsPage: кнопки «⤓ PDF / ⤓ Excel / ⤓ PNG». Проверено СКВОЗНО (xlsx 200, 4 листа: Сводка KPI=140, график Паспорт=140, динамика 100/120/140, таблица) и ВЖИВУЮ (Excel→200 через прокси, PNG→консоль чистая; кнопки видны). tsc/build 0. Замечание про киоск (объяснено пользователю): витрина = полноэкранная веб-страница, НЕ выбирает монитор; вывод на ТВ = отдельное устройство-плеер (мини-ПК/Android-box/RPi на Astra) с браузером в kiosk-режиме на URL дашборда; для «боевого» ТВ рекомендованы (позже, волна B): deep-link `/kiosk/{id}` + токен экрана/гостевой read-only вход без пароля. ДАЛЬШЕ по ревизии (docs/Ревизия_2026-07-18.md): волна A (графичность конструктора) или волна B (админ/модерация/аудит) |
| 2026-07-18 | Бэклог §7 приоритет 2 — Режим-витрина / киоск (требование: ТВ в холле МФЦ). Новый компонент `KioskView` (фронт, без бэкенда): полноэкранный тёмный оверлей (z-index 200) + попытка Fullscreen API, автопрокрутка страниц по таймеру (интервал 10/15/20/30/60с), пауза, ручная навигация (кнопки ‹ › + стрелки ←→ + пробел-пауза, Esc-выход), индикатор страниц (точки), живые часы, авто-обновление данных виджетов раз в 60с (reloadKey). Виджеты рендерятся в исходной раскладке (react-grid-layout static) через WidgetView в режиме только-просмотр (showDrill=false). Кнопка «📺 Витрина» в шапке дашборда — доступна ВСЕМ, кто видит дашборд (не только manage). Проверено ВЖИВУЮ: «Динамика тест» (1 стр.) — полный экран, карточки KPI/график/динамика/таблица, шапка+часы+управление; «Показатели МФЦ» (2 стр.) — индикатор из 2 точек, ручное переключение Обзор→Динамика (круговая «Доля услуг»), активная точка сместилась. Консоль чистая, tsc/build 0. ДАЛЬШЕ (последнее в приоритете 2 §7): экспорт Excel/PNG |
| 2026-07-18 | Бэклог §7 приоритет 2 — Виджеты-аннотации (пожелание 2: читаемые дашборды). Два новых типа виджетов БЕЗ данных: `text` (заголовок крупно + текст + выравнивание left/center) и `image` (URL картинки/лого + подпись; поддержка https и data:URI, object-fit contain). Бэкенд: +`text`/`image` в WIDGET_TYPES (+ANNOTATION_TYPES); `compute_widget_data` отдаёт их config напрямую (данные/датасет не нужны). Фронт: WT +2 типа, DEFAULT_SIZE; WidgetForm — поля Заголовок/Текст/Выравнивание (text) и URL/Подпись (image), сборка config; WidgetView — рендер аннотаций (heading 18px bold + body, картинка с подписью), drill-кнопка скрыта для text/image. Работают и в конструкторе (создание), и в модалке «✎» (редактирование, предзаполнение). Проверено СКВОЗНО (создание text/image → data OK) и ВЖИВУЮ (текст «Итоги квартала» по центру + «Данные по объекту МФЦ»; картинка с подписью «Логотип МФЦ»; редактор аннотации предзаполнен; drill отсутствует; консоль чистая, tsc/build 0). Тестовые виджеты удалены. ДАЛЬШЕ (приоритет 2 §7): режим-витрина/киоск (ТВ в холле МФЦ), экспорт Excel/PNG |
| 2026-07-17 | Бэклог §7 приоритет 2 — Редактирование виджета (закрывает пункт №1 слабых мест: раньше сменить датасет/поле/тип можно было только удалив и создав заново). Бэкенд: PATCH `/widgets/{id}` теперь принимает `widget_type` (валидация по WIDGET_TYPES; `update_widget` — `widget_type=coalesce(...)`); api.ts updateWidget +widget_type. Фронт: `WidgetForm` обобщён (props `initial?: Widget`, `submitLabel`) — переиспуется и для создания (инлайн), и для редактирования (модалка), предзаполняется из config; при смене привязки СОХРАНЯЕТ условное форматирование (`alerts`/`alert_on`), если новый тип его поддерживает; размещение (позиция/размер) не трогается. Кнопка «✎» в шапке kpi/plan_fact/dynamics/…любого виджета → модалка «Изменить виджет». Проверено СКВОЗНО (kpi→bar смена типа+имени, неизвестный тип→400, алерт сохранён при смене поля) и ВЖИВУЮ (открыл редактор — предзаполнен; сменил тип KPI→Столбцы→бейдж и график обновились; консоль чистая, tsc/build 0). Тестовый виджет возвращён в исходный KPI. ДАЛЬШЕ (приоритет 2 §7): виджеты-аннотации (заголовок/текст/лого), режим-витрина/киоск, экспорт Excel/PNG |
| 2026-07-17 | Бэклог §7 П3 — Глобальные фильтры дашборда + пресеты (FR-13). Панель фильтров страницы объединена: период (from/to) + «Строка» (категория = row_label, выпадашка из строк датасетов виджетов страницы, либо свободный ввод; синхронизирована с cross-filter по клику) + общий «сброс». Пресеты (сохранённые наборы): миграция 015 `dashboard_filter_presets` (dashboard_id, name, filters jsonb {from,to,row}, unique(dashboard_id,name)). Бэкенд: `list_presets` (по праву просмотра — RLS), `create_preset` (manage; whitelist ключей, дедуп), `delete_preset` (manage); роутер `GET /dashboards/{id}/presets` (view), `POST` + `DELETE .../{preset_id}` (manage; вложены под dashboard_id). Фронт: api.ts (+listPresets/createPreset/deletePreset, типы DashFilters/DashPreset); DashboardsPage — строка «Пресеты» (чипы-применение, ✕-удаление для manage, «💾 Сохранить текущие» → prompt имени); применение пресета выставляет период+строку, виджеты перезапрашиваются. Проверено СКВОЗНО (создание/дубль→400/список/удаление→204/фильтры сохраняются) и ВЖИВУЮ (пресет «Только Паспорт» → период 01.01.2026 + Строка=Паспорт, виджеты отфильтровались; консоль чистая, tsc/build 0). Тестовый пресет удалён. **ПЕРВАЯ ВОЛНА §7 ЗАКРЫТА** (KPI-алерты ✅ → RLS ✅ → фильтры/пресеты ✅). На будущее (§7): виджетные фильтры (переопределение глобальных), фильтр по объекту, батч-эндпоинт данных страницы + кэш, редактирование привязки виджета |
| 2026-07-17 | Бэклог §7 П2 — Разграничение доступа к дашбордам (RLS через существующий ACL `access_grants`, без миграции). Бэкенд (`dashboards/service.py`): `PRIVILEGED_ROLES={admin,moderator,senior_moderator}` видят всё; остальные — дашборды, созданные ими (`created_by`) или с грантом `scope='dashboard'` на их user_id/роль. Хелперы `visible_dashboard_ids` / `_can_view`; фильтрация в `list_dashboards`, `get_dashboard` (404 если нет доступа), `list_page_widgets`, `compute_widget_data` (user опционален — для внутр. вызова из алертов), `widget_drill`, `list_versions`, `list_org_alerts` (алерты только по доступным). Гранты: `grant_targets` (пользователи+роли орг.), `list_grants`, `add_grant` (валидация роль/пользователь принадлежат орг., дедуп «уже выдан»), `remove_grant`. Роутер: `GET/POST /dashboards/{id}/grants`, `DELETE /dashboards/{id}/grants/{grant_id}` (все manage; вложены под dashboard_id — без коллизий маршрутов и без правки vite-прокси). `home/service.get_home` тоже фильтрует каталог/счётчик дашбордов/алерты. Фронт: api.ts (+listDashboardGrants/addDashboardGrant/removeDashboardGrant, типы DashGrant/GrantTargets); DashboardsPage — кнопка «🔒 Доступ» + модалка AccessEditor (чипы ролей/пользователей 👥/👤, выдать/убрать). Проверено СКВОЗНО на тестовом пользователе `viewer1` (роль user): без гранта — /dashboards=0, /home dashboards=0/pages=0/alerts=0; чужой дашборд/виджет/drill → 404; управление → 403; после гранта — виден 1 дашборд, данные виджета читаются; дубль-грант → 400. ВЖИВУЮ: модалка доступа, добавление гранта по роли (Аналитик) и удаление, консоль чистая, tsc/build 0. Тестовый пользователь удалён. ОСТАЛОСЬ по §7 первой волны: глобальные фильтры + пресеты. NB на будущее: RLS пока dashboard-level (widget-level whitelist из миграции 004 не задействован); публикация/модерация не enforced |
| 2026-07-17 | Бэклог §7 П1а — KPI-алерты + условное форматирование (без миграции, пороги в `config` виджета). Бэкенд (`dashboards/service.py`): движок `evaluate_alert` — правила `config.alerts=[{level danger/warn/good, op lt/lte/gt/gte/eq/between/outside, value, value2?, label?}]`, сравнение по `alert_on` (kpi→value; plan_fact→pct/fact/delta/plan; dynamics→last/change/change_pct); первое подходящее правило задаёт цвет/подпись; алерт вложен в данные виджета (`data.alert`). `list_org_alerts` — сканирует виджеты с порогами по орг., возвращает сработавшие warn/danger (danger первыми) для «Главной»; подключено в `home/service.py` (блок `alerts` больше не пустой). Фронт: `WidgetView` — подсветка тела (рамка/фон/бейдж «⚠ …»), число KPI и полоса план-факта в цвете уровня; `DashboardsPage` — модалка «⚠ Пороги» (кнопка в шапке kpi/plan_fact/dynamics) с добавлением/правкой/удалением правил → PATCH config; `HomePage` — блок «⚠ Требуют внимания (N)» (кликабельные карточки → открыть дашборд, черновики помечены). Проверено СКВОЗНО (порог danger `<1e8`→сработал 140, /home alerts=1) и ВЖИВУЮ (KPI красный «Ниже нормы»→правка порога `<100`→мгновенно зелёный «≥0»; good отфильтрован из «Главной»=0; консоль чистая, tsc/build 0). ОСТАЛОСЬ по §7 первой волны: доступ к дашбордам (RLS через ACL) → глобальные фильтры + пресеты |
| 2026-07-12 | Этап 5.5 («Главная»): миграция 013 (`home_kpis` — выбор ключевых KPI админом). Модуль `home` — GET /home (счётчики дашборды/объекты/метрики/датасеты/пользователи; каталог страниц; лента «что нового»; свежесть данных по объектам; ключевые KPI со значениями), POST/DELETE /home/kpis. Фронт: HomePage (гибрид) — приветствие+живые дата/время, счётчики, ключевые KPI (админ добавляет/убирает), кликабельный каталог страниц→переход в дашборд, «что нового»+«свежесть». App: раздел «Главная» первый и стартовый; переход из каталога (openDash→DashboardsPage.initialDashboardId). vite-прокси +/home (был баг — HTML вместо JSON, исправлено). Проверено ВЖИВУЮ: счётчики 2/3/4/4/1, KPI 150/106.67, каталог+переход, что нового, свежесть; консоль чистая, tsc/build 0. ОСТАЛОСЬ по этапу 5: сравнение периодов (пожелание 1) + движок KPI-алертов (блок alerts пока пуст). Модель дашбордов ГОТОВА (0/1/3/4/5) |
| 2026-07-12 | Этап 5.3 (Viewer: ECharts + drill): подключён `echarts` — виджеты столбцы/линия/круговая рендерятся настоящими графиками (`EChart` обёртка, `WidgetView` обновлён; KPI/таблица/план-факт остаются HTML). Прозрачность (drill): бэкенд GET `/widgets/{id}/drill` (формулы метрик — уровень 1 + первичные строки датасетов — уровень 2), фронт — модалка «Из чего собран» по кнопке «🔍 подробнее». Проверено ВЖИВУЮ: столбчатый график Паспорт/ИНН, drill KPI «Итого план» → формула SUM(field('plan','kol')) + первичные строки (Паспорт 100/ИНН 50). Консоль чистая, tsc/build 0 (echarts даёт большой чанк — для on-prem ок). ОСТАЛОСЬ по этапу 5: фильтр-период + сравнение периодов (пожелание 1; нужен ряд по периодам/PERIOD_COMPARE — сделать отдельным шагом), и 5.5 «Главная» |
| 2026-07-12 | Этап 5.4 (фронтенд, конструктор дашбордов): раздел «Дашборды» (DashboardsPage) — список+создание дашбордов; **уровень СТРАНИЦ виден и управляем** (вкладки страниц, создание/удаление именованных страниц); добавление виджетов на страницу с выбором типа (KPI/столбцы/линия/круговая/таблица/план-факт), источника (метрика/датасет) и полей через выпадашки. Компонент `WidgetView` — лёгкий рендер данных (KPI/бары/таблица/план-факт с прогрессом; ECharts будет в 5.3). api.ts +типы/функции dashboards; App: NAV «Дашборды» ready; vite-прокси +/dashboards,/dashboard-pages,/widgets. Проверено ВЖИВУЮ: дашборд «Показатели МФЦ»→страница «Обзор» (KPI 150, столбцы, таблица, план-факт 71.11%); создал страницу «Динамика»+виджет «Доля услуг» (круговая: Паспорт 62%/ИНН 23%/СНИЛС 15%). Консоль чистая, tsc/build 0. Дальше: 5.3 Viewer (ECharts+фильтр-период+drill), 5.5 Главная |
| 2026-07-12 | Этап 5.0+5.1 (бэкенд дашбордов): миграция 012 (`dashboard_pages` — дашборд→именованные страницы; `widgets.page_id`). Модуль `dashboards` — CRUD дашбордов/страниц/виджетов + вычисление данных виджета: kpi (из метрики/датасета), table, bar/line/pie (категория=row_label, значение=поле), plan_fact (метрики или поля датасета). Выбор версии метрики для виджета: одобренная→проверенная→любая. Роутер: /dashboards, /dashboards/{id}/pages, /dashboard-pages/{id} (patch/delete/widgets), /widgets/{id} (patch/delete/data). Проверено СКВОЗНО (13/13): дашборд→страница→4 виджета; KPI=150, график [Паспорт,ИНН][100,50], таблица 2 стр., план-факт pct=71.11. Дальше: 5.2 страницы/дашборды (доп.), 5.3 фронтенд-Viewer (ECharts), 5.4 конструктор, 5.5 Главная |
| 2026-07-12 | Наглядность источника в конструкторе: `/metrics/data-sources` теперь отдаёт по датасету `folder` и `document` (исходный файл, через source_document_version_id→document_versions→documents→folders). В выпадашке подпись «название (код) — объект / папка» + строка-источник «📄 файл · папка · объект» — прямой ответ «какой датасет из какого документа». Проверено вживую (s32 (dataset) — Тест 3.2 / Услуги; 📄 s32.csv), консоль чистая, tsc/build 0 |
| 2026-07-12 | Этап 4.4 (визуальный конструктор формул): бэкенд — GET `/metrics/data-sources` (датасеты с полями/строками/датами + метрики; маршрут ДО `/{metric_id}`, иначе перехват). Фронт — компонент `FormulaBuilder`: сбор формулы мышью из «элементов» (агрегат столбца / ячейка / метрика / число), соединение действиями кнопками `＋ − × ÷`, добавление/удаление элементов, выбор данных из выпадашек; генерация DSL «на лету». В MetricsPage — переключатель «🖱 Конструктор / ⌨ Текст» (общее поле формулы, синхронно). Проверено ВЖИВУЮ: собрал `SUM(field('dataset','prinyato')) + SUM(...)` мышью → предпросмотр 390, переключение в текст сохраняет формулу, консоль чистая, tsc/build 0 ошибок. Пожелание «выбор данных/действий мышью» закрыто |
| 2026-07-12 | Документация формул: создана отдельная инструкция `docs/Инструкция_по_формулам.md` (где писать, кирпичики данные/действия/функции/фильтры, примеры МФЦ, предпросмотр, версии/статусы, ограничения — оконные функции, частые ошибки). В UI (MetricsPage) расширены подсказки: строка-подсказка под полем формулы + сворачиваемый «📘 Справочник по формулам» (данные/действия/функции/фильтр/примеры-клик). Проверено вживую (справочник рендерится, консоль чистая). ВИЗУАЛЬНЫЙ конструктор формул (выбор данных/действий мышкой, без текста) — запланирован ОТДЕЛЬНЫМ под-этапом 4.4 (док-07 «два режима»), пока не сделан |
| 2026-07-25 | КАЧЕСТВО — ФРОНТЕНД-ТЕСТЫ (Vitest), закрыт пробел покрытия (был только tsc/build). Настройка: vitest+@testing-library/react+jest-dom+jsdom (devDeps), `test` секция в vite.config.ts (jsdom/globals/setup src/test/setup.ts), скрипт `npm test`=vitest run. Тесты: `api/auth.test.ts` (7 — checkPassword: короткий/без-цифры/без-буквы/=логину/стойкий/complexity=false + passwordHint), `WidgetPicker.test.tsx` (4 — 4 группы рендерятся; новые типы heatmap/pivot/waterfall/сравнение-подразделений видны; клик по карточке→onPick(тип)+onClose; WIDGET_META согласован). **11 тестов зелёные.** CI: frontend-job теперь tsc→**npm test**→build. Проверено (tsc 0; 11 тестов pass; build 0). **ВЕСЬ СПИСОК ПОЛЬЗОВАТЕЛЯ ЗАКРЫТ:** оконные функции ✅ · блокировка входа ✅ · батч-эндпоинт ✅ · фронт-тесты ✅ (+ранее логин-страница/подсказки пароля/проверка GitHub). ДАЛЬШЕ (остаток бэклога): год-к-году полноценный · RLS на строки/widget-level · индексы/пагинация · рассылка PDF · комментарии · [деплой] HTTPS/наблюдаемость · брендбук. |
| 2026-07-25 | ПЕРФ — БАТЧ-ЭНДПОИНТ данных страницы (1 запрос вместо N; дополняет TTL-кэш). Бэк: `_widgetdata.compute_page_data` (доступ проверяется 1 раз на странице; далее `compute_widget_data(skip_acl=True)` по каждому виджету — переиспользует кэш+алерты; ошибка одного виджета → `{id,error}`, не рушит остальные), эндпоинт `GET /dashboard-pages/{page_id}/data?from&to&row` (учитывает фильтры страницы; объявлен рядом с /widgets — под тем же корнем, прокси не нужен). Фронт: `getPageData` (api); DashboardsPage — эффект грузит батч по [page,from,to,row,reloadKey] в map, виджеты ВСЕГДА в батч-режиме (WidgetView +пропы `batched/injData/injError` — не фетчит сам, ждёт данные родителя), при СБОЕ батча фолбэк `batchFailed`→self-fetch. Проверено СКВОЗНО (61 тест, +2: батч 2 виджета kpi+table, несущ. страница→404) и ВЖИВУЮ (по Performance API: **1 запрос /data, 0 per-widget /widgets/{id}/data**; дашборд «Показатели МФЦ» отрисован — KPI 150/таблица/план-факт; консоль чистая). Cross-filter/фильтры-периода перезапрашивают батч. tsc/build 0. ДАЛЬШЕ: фронт-тесты (Vitest/Playwright). |
| 2026-07-25 | БЕЗОПАСНОСТЬ — блокировка входа после N неудач (защита от подбора). В `/auth/login` перед проверкой пароля считаются неудачные попытки по логину за окно (`login_events`, `success=false`, `created_at > now()-make_interval(mins)`); при `>= LOGIN_MAX_ATTEMPTS`(5) за `LOGIN_LOCKOUT_MINUTES`(15) — **429** «Слишком много попыток, повторите через N мин», даже с верным паролем; окно скользящее — самоснимается. Config +2 поля (0 в max — выключить). Блокировка по ЛОГИНУ (для LAN/госсистемы ок; NB: теоретически DoS-лок чужого логина спамом — приемлемо для внутренней сети). Проверено СКВОЗНО (тест на уникальном логине: 5×401→6-я 429, с очисткой; admin не затронут; 59 тестов) и ВЖИВУЮ (5×401→429; admin входит 200; тест-шум login_events очищен). `.env.prod.example` +LOGIN_MAX_ATTEMPTS/MINUTES. ДАЛЬШЕ: батч-эндпоинт данных страницы → фронт-тесты. |
| 2026-07-25 | ДВИЖОК МЕТРИК — ОКОННЫЕ ФУНКЦИИ теперь ВЫЧИСЛЯЮТСЯ (был пробел: парсились, но не считались — заглушка). Реализован период-осведомлённый расчёт: `resolver._dataset_periods` (ось времени из reporting_period_start выпусков), `_fetch_column(..., period=)` (значения выпуска за период), `_compute_window_series` (arg считается для каждого периода → ряд [(период,значение)]; metric/cell внутри окна — на активном выпуске). `collect_refs` собирает оконные узлы и не рекурсирует в них; `CacheResolver.window_series` отдаёт предзагруженный ряд по ключу узла (`evaluator.node_key`). Семантика (evaluator): **RUNNING_TOTAL**(arg,grain)=сумма по всем периодам; **PERIOD_COMPARE**(arg,unit,mode)=последний период vs на 1 unit назад (delta/pct/ratio; day/week/month/quarter/year; `_shift_back`+`_nearest_value`) — это «месяц-к-месяцу/год-к-году»; **SHARE_OF_TOTAL**(arg,over)=доля последнего периода в сумме по периодам, %. In-memory резолвер (смоук) без периодов — по-прежнему бросает «требует ряд значений по периодам» (смоук 38 ок цел). Проверено СКВОЗНО (preview на nagruzka 100/120/140: RUNNING_TOTAL=360, PERIOD_COMPARE delta=20/pct=116.67, SHARE=38.89; 57 тестов, +5 test_windowing на seed 165/180; оконная в выражении `/2`) и статикой (py OK). docs/Инструкция_по_формулам.md — оконные перенесены из «ограничений» в рабочий раздел с примерами. Закрывает пробел движка + частично «год-к-году» (PERIOD_COMPARE unit='year'). ДАЛЬШЕ: блокировка входа после N неудач → батч-эндпоинт → фронт-тесты. |
| 2026-07-25 | UX/БЕЗОПАСНОСТЬ — стартовая страница + подсказки пароля + проверка GitHub. ① **Логин-страница** (`Login.tsx`) переделана в двухпанельную под стиль МФЦ (легко перебрендировать — цвета/название/эмблема в объекте `BRAND`): слева синий градиент-герой «Аналитический портал для управленческих решений» + описание портала + 4 карточки возможностей (дашборды/прозрачность/аналитика/отчёты), справа — форма входа; адаптив (локальный хук useNarrow ≤900px → вертикальный стек). ② **Подсказки парольной политики:** публичный эндпоинт `GET /auth/password-policy` ({min_length, require_complexity}); фронт-хелперы `getPasswordPolicy/passwordHint/checkPassword` (api/auth.ts); в ChangePassword и UsersPage (создание) — подсказка «минимум 8, буквы и цифры, не совпадает с логином» + ЖИВАЯ предпроверка под полем (красным при нарушении); prompt сброса пароля обновлён. Убраны устаревшие «мин.6/мин.4». ③ **Проверка GitHub:** remote github.com/denregeda/dashbord, ветка etap-2-objects синхронна (0/0), gh авторизован, CI success. NB: MCP-коннектор GitHub (ревью PR из Claude) требует авторизации, но git+gh+CI работают; внешние SaaS намеренно не подключаем (on-prem). Проверено ВЖИВУЮ (десктоп двухпанельный + моб-стек; вход работает; подсказка + живая ошибка «минимум 8»; консоль чистая) и СКВОЗНО (policy endpoint {8,true}; 52 теста, +1). tsc/build 0. Брендбук МФЦ ждём — тогда заменить BRAND/эмблему. |
| 2026-07-25 | БЭКЛОГ #4 — ВАЛИДАЦИЯ ПРИ ЗАГРУЗКЕ ДАННЫХ (бизнес-правила). При выпуске датасета (`ingestion/mapping.build_release`) прогоняется НЕблокирующая проверка: чистая функция `_validate_grid(rows, value_fields, label_col, field_type)` собирает предупреждения — дубли названий строк, строки без названия, «число не распознано» (текст в числовом поле), отрицательные значения, пропущенные числовые. Данные грузятся, но `build_release` возвращает `validation:{warnings:[{code,count,message}], ok}`. Фронт (ExtractionPage `ResultPanel`) после выпуска показывает жёлтый блок «⚠ Проверка данных: замечания (N)» со списком либо «✓ без замечаний». Тип `ValidationWarning`/`validation?` в api. Логика вынесена в чистую функцию → 7 юнит-тестов (test_validation.py: clean/дубли/пустые/текст/отрицательные/пропуски/комбо). Проверено СКВОЗНО (51 тест зелёный, +7; API жив после правки mapping — reload ок) и статикой (tsc/build 0). NB: полный upload→extract→release e2e не гонял (тяжело без реального документа) — логика покрыта юнит-тестами, проводка проверена tsc+parse. **БЭКЛОГ ИЗ 4 ПУНКТОВ ЗАКРЫТ** (сравнение подразделений ✅ · экспорт журналов ✅ · цели/тренд ✅ · валидация ✅). |
| 2026-07-25 | БЭКЛОГ #3 — ЦЕЛИ/БЕНЧМАРКИ + ЛИНЕЙНЫЙ ТРЕНД (без ИИ), как настройки существующих виджетов. **Цель (target):** KPI/gauge — `config.target` (+`target_label`); бэк `_apply_target` добавляет target и `target_pct=value/target*100`; фронт компонент `TargetLine` под значением («Цель: X · N%», зелёный+✓ при ≥100%). **Тренд:** «Динамика» — `config.trend=true`; бэк `_linear_trend` (метод наименьших квадратов y=a+b·x, возвращает slope + концы прямой); фронт накладывает золотую пунктирную линию «Тренд» (интерполяция по периодам) + легенда + текст «тренд: ↗ рост/↘ спад (slope/период)». Форма: поле «Цель» (kpi/gauge) + галочка «Линия тренда» (dynamics). Год-к-году НЕ делал отдельно (частично покрыт «Динамикой»+сравнением периодов; полноценный — когда данные охватят годы, точечно). Проверено СКВОЗНО (preview: KPI total_plan цель200→75%; dynamics nagruzka trend [100→140] slope20; 44 теста, +2 на target/trend) и ВЖИВУЮ (динамика с золотой линией тренда «↗ рост 20/период» + легенда; KPI «140 · Цель: 200 · 70%»; консоль чистая). Тест-виджеты удалены. tsc/build 0. ДАЛЬШЕ по бэклогу: валидация при загрузке данных (последнее из 4). |
| 2026-07-25 | БЭКЛОГ #2 — ЭКСПОРТ ЖУРНАЛОВ CSV/Excel (аудит действий + журнал входов). Общий util `app/exports.py`: `to_csv` (разделитель `;` + BOM UTF-8 → Excel-RU открывает без «кракозябр»/мастера импорта), `to_xlsx` (openpyxl). Аудит: `audit.service.export_events` (рефактор — вынес `_events_where`, общий для чтения и экспорта; те же фильтры; до EXPORT_MAX=50000 строк; действия по-русски ACTION_RU) + эндпоинты `GET /audit/export.csv|xlsx` (ДО `/audit/{id}` — иначе перехват). Входы: `users.service.login_events_export` (все события) + `GET /login-events/export.csv|xlsx`. Все admin-only. Фронт: `downloadFile` в http.ts (blob+auth→скачивание), `exportAudit(q,fmt)` (с текущими фильтрами) + `exportLoginEvents(fmt)`; кнопки «⤓ CSV/⤓ Excel» в шапке AuditPage и в разделе «Журнал входов» UsersPage. Проверено СКВОЗНО (csv 200 с BOM+`;`+кириллицей; xlsx=валидный Microsoft Excel; фильтры работают; без токена→401; viewer→403; /audit после рефактора цел; 42 теста зелёные, +5 на экспорт) и ВЖИВУЮ (кнопки на «Аудит» отрисованы; консоль чистая). tsc/build 0. ДАЛЬШЕ по бэклогу: цели/бенчмарки+тренд → валидация загрузки. |
| 2026-07-25 | БЭКЛОГ #1 — ВИДЖЕТ «Сравнение подразделений» (`objects_compare`), 15-й тип. Реальная «подразделенческая» ось в модели = ОБЪЕКТЫ (каждый объект=подразделение/филиал МФЦ; датасеты привязаны через object_id; таблица departments пуста). Виджет агрегирует выбранный показатель (canonical field) по объектам: для каждого объекта берётся ПОСЛЕДНИЙ активный выпуск (distinct on object_id order by period desc) → сумма поля → столбцы с названиями объектов. Бэк: ветка в `_widgetdata._compute_widget` (config {value_field}); xlsx-лист «Подразделение/Значение». Фронт: пикер (группа Графики, +SVG-иконка), WT/DEFAULT_SIZE; в WidgetForm СВОЙ селектор «Показатель (по подразделениям)» из уникальных числовых полей ВСЕХ датасетов (allNumFields, без привязки к датасету); WidgetView — столбцы с палитрой по объектам + подписи. Тест-фикстура seed_dataset дополнена тест-объектом t_obj (релизы привязаны к нему). Проверено СКВОЗНО (preview по prinyato → Тест 3.2:195, Динамика тест:140; негатив без поля→400; 37 тестов зелёные) и ВЖИВУЮ (галерея 15 карточек; виджет добавлен и отрисован — столбцы Тест 3.2/Динамика тест 195/140 с подписями; консоль чистая). Тест-виджет удалён. tsc/build 0. ДАЛЬШЕ по бэклогу: экспорт журналов CSV/Excel → цели/тренд → валидация загрузки. |
| 2026-07-25 | ВИДЖЕТЫ +2: Pivot (сводная с итогами) и Waterfall (водопад) — выбраны как наиболее ценные для отчётности МФЦ. Теперь **14 типов**. **Pivot** — HTML-таблица строки×поля + автоитоги: столбец «Итого» по строкам, строка «Итого» по столбцам, общий итог (бэк: ветка в `_widgetdata._compute_widget` через `_dataset_multi_series` → columns/rows[{row,values,total}]/col_totals/grand_total; фронт: рендер таблицы с жирным футером). **Waterfall** — вклад строк в накопленный итог + финальный столбец «Итого» (бэк: через `_dataset_series` → categories/values; фронт: ECharts stacked-bar с прозрачным placeholder-рядом для «плавающих» столбцов, зелёный +/красный −/синий итог, echarts bar уже зарегистрирован — новых компонентов не нужно). Конфиг: pivot как heatmap (dataset+value_fields, `usesMulti`), waterfall как bar (dataset+value_field, `usesValueField`). Добавлены в WIDGET_TYPES(_base), пикер (pivot→Матрицы, waterfall→Графики, +SVG-иконки), WT/DEFAULT_SIZE, xlsx-экспорт, авто-предложения suggest_widgets, тесты (35 всего). Проверено СКВОЗНО (preview: pivot итоги строк 238/90/59 столбцы 195/192 общий 387; waterfall cats/vals; негатив→400; 35 тестов зелёные) и ВЖИВУЮ (галерея 14 карточек с превью; оба виджета добавлены на дашборд и отрисованы — водопад со ступенчатыми зелёными столбцами+синий Итого 195, сводная с футером Итого 195/192 и столбцом Итого по строкам; консоль чистая). Тест-виджеты удалены (204). tsc/build 0. Осталось из «кандидатов»: гео-карта/radar/scatter/sankey/gantt — только точечно по запросу (для МФЦ избыточны). |
| 2026-07-25 | БЕЗОПАСНОСТЬ — парольные политики (пункт 4 ревизии, реализуемая часть). Единый валидатор `validate_password(pw, login?)` в auth/security.py: мин. длина (`PASSWORD_MIN_LENGTH`=8), сложность буквы+цифры (`PASSWORD_REQUIRE_COMPLEXITY`), не совпадать с логином, не из списка распространённых слабых. Применён в 3 точках, где раньше было слабо/непоследовательно (change-password мин.6, create_user/reset мин.4): auth `/change-password` (→400 с сообщением), users `create_user`/`reset_password` (→UsersError). НЕ применяется к первичному admin из bootstrap (ADMIN_PASSWORD задаёт деплойщик). Config +2 поля. Тест `test_password.py` (short/no-digit/common→400, стойкий→200) — теперь **31 тест зелёный**. Проверено вживую (слабый→400 с русским сообщением, admin-вход цел). `.env.prod.example` +PASSWORD_*; `docs/ДЕПЛОЙ.md` раздел «Безопасность» обновлён (политика ✅, автобэкап ✅; **MFA/SSO через Keycloak и HTTPS-терминация на прокси — роадмап деплой-фазы**, до этого стойкие пароли + блокировка по login_events). **ПЛАН НАДЁЖНОСТИ ИЗ РЕВИЗИИ ЗАКРЫТ:** ① автотесты+CI ✅ ② рефактор service.py ✅ ③ автобэкап-cron ✅ ④ безопасность: пароли ✅, MFA/HTTPS отложены осознанно. Плюс dev hot-reload ✅ и UUID-харденинг ✅. |
| 2026-07-25 | НАДЁЖНОСТЬ — автобэкап по расписанию (`backup-schedule.sh`). Обёртка над `backup.sh` (pg_dump+MinIO tar+ротация): ставит systemd-таймер `dashbord-backup.timer` (`Persistent=true` — догоняет пропущенный по выключению ВМ запуск), fallback на cron если нет systemd. Команды install/uninstall/status/print (print — dry-run без прав). Время/хранение/каталог — `BACKUP_TIME`(03:30)/`BACKUP_KEEP`(7)/`BACKUP_DIR` из env или `.env.prod`. Валидирует формат HH:MM. Проверено: `bash -n` ок, print генерирует корректные unit'ы (oneshot service + OnCalendar daily timer) и cron-строку, читает `.env.prod`. `.env.prod.example` +BACKUP_TIME/KEEP, `docs/ДЕПЛОЙ.md` +раздел. (Реально ставится только на Linux-хосте с systemd/cron — на dev-macOS проверена генерация.) |
| 2026-07-25 | ТЕХДОЛГ — рефактор `dashboards/service.py` (1166→569 стр., >2× меньше) под защитой 27 тестов. Разбит на слои-соседи, `service.py` остаётся ФАСАДОМ-барлером (реэкспорт всех имён, включая `_`-функции → внешние вызовы router/home/reports/moderation работают без правок): `_base.py` (DashboardError, WIDGET_TYPES/ANNOTATION_TYPES — лист), `_alerts.py` (KPI-алерты + `_cfg`), `_rls.py` (видимость: `visible_dashboard_ids`/`_can_view`/`_user_ctx`), `_widgetdata.py` (вычисление данных: метрики/датасеты/`_compute_widget`/`compute_widget_data`+кэш/`preview_widget`/`list_org_alerts`/`export_page_xlsx`/`widget_drill` + `_widget_org`/`_page_org`). Переносил СКРИПТОМ (точная нарезка строк, без ручных ошибок), инкрементально с прогоном тестов после каждого шага. Проверено СКВОЗНО (27 тестов зелёные после каждого извлечения) и ВЖИВУЮ (home/reports/данные виджета/drill/**export.xlsx**/versions/suggest/auto_build — все 200/201). Живой e2e поймал баг, который юнит-тесты не покрыли: `export_page_xlsx` звал `_page_org`, оставшийся в фасаде → NameError; исправлено переносом `_page_org` в `_widgetdata` + реэкспорт (урок: после рефактора гонять НЕ только юнит-тесты, но и живой e2e непокрытых путей). ОСТАЛОСЬ (по желанию, малый приоритет): гранты/пресеты/версии/шаблоны ещё в service.py (569 стр. — норма). ДАЛЬШЕ по плану надёжности: автобэкап-cron → парольные политики. |
| 2026-07-25 | НАДЁЖНОСТЬ — слой автотестов (pytest+httpx) в CI + dev hot-reload (закрытие главного риска ревизии: раньше CI гонял лишь синтаксис/сборку, не поведение). **Тесты (backend/tests/):** conftest.py — пул БД напрямую (без redis/minio, кэш деградирует мягко), фикстуры admin/viewer(роль user)/moderator, минимальный датасет `t_ds` (2 выпуска×3 строки×поля plan/fact), `purge_dashboard` (жёсткая очистка со всеми FK-детьми — API-delete дашборда НЕТ by design) + автоочистка `ztest_*` в начале сессии. test_auth (вход/401/me/невалидный-UUID→400), test_widgets (**все 12 типов** через /widgets/preview: значения/структура + негативы 400), test_rls (viewer не видит чужой draft→404; после гранта+ПУБЛИКАЦИИ видит; не-manage→403), test_moderation (submit→очередь→approve ДРУГИМ модератором; свой одобрить нельзя→400; двойной submit→400). Настройка: pytest.ini (asyncio_mode=auto, session-loop — иначе «attached to different loop»), requirements-dev.txt (в прод-образ НЕ идёт). **27 тестов зелёные** и локально (dev-БД), и против ЧИСТОЙ БД (CI-паритет: миграции+ensure_seed, тесты самодостаточны, самоочистка). Тесты поймали и зафиксировали реальное (корректное) поведение: разделение обязанностей при модерации + модерационный гейт видимости. **CI:** job `tests` (postgres:16 + миграции + requirements-dev + pytest). **Dev DX:** docker-compose.yml — volume-mount `./backend/app` + uvicorn `--reload` (api) и arq `--watch` (worker): правки подхватываются без пересборки образа (прод-стек это НЕ использует). ДАЛЬШЕ по плану надёжности: рефактор service.py (под защитой тестов) → автобэкап-cron → парольные политики. |
| 2026-07-25 | ПЕРФ — code-splitting фронт-бандла (рекомендация ревизии; важно для слабого железа/LAN МФЦ). Было: один чанк 2008 КБ (625 КБ gzip) грузился при входе. Стало: **начальная загрузка ~411 КБ (~116 КБ gzip, −81%)**. Что сделано: (1) `EChartLazy.tsx` — общая ленивая обёртка (`React.lazy`+`Suspense` «Загрузка графика…»); `WidgetView`/`ReportsPage` импортируют её вместо прямого `./EChart` (иначе статический импорт из ReportsPage тянул echarts в главный чанк) → echarts уехал в отдельный чанк, грузится лишь при появлении графика; (2) `EChart.tsx` переведён на модульную сборку `echarts/core` с точечной регистрацией (BarChart/LineChart/PieChart/GaugeChart/HeatmapChart + Grid/Tooltip/Legend/Title/VisualMap + CanvasRenderer) → чанк echarts 1033→**575 КБ** (192 КБ gzip); (3) `jspdf`+`html2canvas` в `DashboardsPage` — динамический `import()` внутри exportPdf/exportPng (грузятся только по клику «⤓ PDF/PNG»); (4) `vite.config.ts` +manualChunks (react-vendor, grid) + chunkSizeWarningLimit 900. Предупреждение о размере чанка ушло. Проверено СКВОЗНО (build: чанки index 191/react 142/grid 77/EChart 575/jspdf 358/html2canvas 202; tsc 0) и ВЖИВУЮ (дашборд «Динамика тест» — 2 графика-canvas 501×200/396×180 отрисованы через ленивый чанк; консоль чистая). ДАЛЬШЕ: рефактор `dashboards/service.py` (отдельным проходом) либо одна из 5 идей архитектора. |
| 2026-07-25 | Виджет-ПИКЕР (галерея) + виджет HEATMAP (согласованный шаг после ревизии проекта). **Пикер:** новый компонент `dashboards/WidgetPicker.tsx` — модальная галерея типов виджетов с мини-превью (лёгкие inline-SVG, без данных), сгруппированная по 4 категориям (Показатели: KPI/спидометр/план-факт; Графики: столбцы/линия/круговая/динамика/сравнение; Матрицы: таблица/тепловая карта; Оформление: текст/картинка); карточки с иконкой+названием+подсказкой, активный тип подсвечен. В `WidgetForm` прежний `<select>` «Тип» заменён кнопкой «▦ галерея» → открывает пикер; выбор закрывает модалку и ставит тип (со сбросом source→metric, если новый тип не поддерживает формулу). Экспорт `WIDGET_META` (v→метаданные) для подписи выбранного типа. **Heatmap:** бэкенд — `heatmap` в `WIDGET_TYPES`; ветка `_compute_widget` (матрица строки×поля через `_dataset_multi_series` → `{rows,columns,cells:[[col,row,val]],min,max}`); лист в xlsx-экспорте; авто-предложение в `suggest_widgets` (≥2 числовых поля). Фронт — `WidgetView` рендер heatmap на ECharts (visualMap-градиент сине-золотой, подписи ячеек при ≤60, адаптивная высота); в `WidgetForm` heatmap использует датасет+мультиполя (как compare, но без выбора viz); `WT`/`DEFAULT_SIZE` +heatmap. Проверено СКВОЗНО (preview на датасете `dataset`: матрица 3×2 rows=[Паспорт РФ,ИНН,СНИЛС] cols=[Принято,Выдано] 6 ячеек min=29/max=120; негатив без полей→400, неизвестный тип→400) и ВЖИВУЮ (галерея-модалка со всеми 4 группами и мини-превью; выбор «Тепловая карта» → форма показала датасет+чекбоксы полей; живой предпросмотр отрисовал ECharts-canvas; виджет добавлен на дашборд «Динамика тест» и отрисован как тепловая карта — строка СНИЛС, ячейки 30/29 в цветовой заливке; консоль чистая). Тестовый виджет удалён (204). tsc/build 0. ДАЛЬШЕ: перф-бандл (code-splitting echarts/jspdf), рефактор `dashboards/service.py` (1178 стр., отдельным проходом), либо одна из 5 идей архитектора |
| 2026-07-25 | ПЕРФ — ИНДЕКСЫ БД + ПАГИНАЦИЯ (фундамент масштабируемости под рост данных МФЦ; выбран пользователем из бэклога). **Миграция 022_perf_indexes.sql** (идемпотентная, `create index if not exists`, БЕЗ concurrently — раннер `run-migrations.sh` применяет файл в транзакции) закрыла запросы, шедшие сканом: `ix_notification_recipients_user (user_id,is_read)` — колокольчик уведомлений на КАЖДОЙ загрузке (был без индекса вовсе); `ix_audit_log_org_created (organization_id,created_at desc)` — базовый список/экспорт аудита (прежние индексы покрывали лишь фильтр по сущности/актору); `ix_documents_folder_created (folder_id,created_at desc)` + `ix_document_versions_doc (document_id,version_no desc)` — список документов и lateral-выбор последней версии; `ix_objects/dashboards/metrics_org_name (organization_id,name)` — базовые списки сущностей. **Пагинация** списка документов в папке `GET /folders/{id}/documents?limit=&offset=` (было: голый массив всех документов) → конверт `{total,limit,offset,items}`, limit 1..200 (деф.50). Фронт: `api/objects.ts` тип `DocPage`+параметры; `ObjectsPage` — состояние `docsTotal`, `loadDocs`/`loadMoreDocs`, кнопка «Показать ещё (N)» (появляется при docs.length<total). Проверено СКВОЗНО (стр.1 limit2→2 из total3; стр.2 offset2→1; limit=0 и limit=999→422; чужая папка→404 RLS; EXPLAIN: Bitmap Index Scan on ix_notification_recipients_user) и ВЖИВУЮ (папка «Нагрузка» — 3 документа отрисованы корректно после смены формата, «Показать ещё» скрыт при 3<50, консоль чистая). Индексы задействованы (проверка планировщика). Тестовых данных не создавал (только чтение). tsc/build 0. ДАЛЬШЕ по бэклогу: RLS на строки/виджеты · рассылка PDF по SMTP · комментарии к дашбордам · онбординг/тултипы |
| 2026-07-25 | БЕЗОПАСНОСТЬ — RLS WIDGET-LEVEL (задействована заготовленная миграция 004, п.3–4; выбран пользователем). Раньше dashboard-level RLS работал, но виджет-гранты (`access_grants.scope='widget'`) не использовались нигде. Теперь: хелпер `_rls.visible_widget_ids(...)` → `None` (видны все виджеты: привилегированный/автор/нет виджет-грантов, fallback п.4) либо `set` разрешённых widget_id (whitelist п.3: как только на дашборде появляется хотя бы один widget-грант, зритель-по-гранту видит ТОЛЬКО выданные ему виджеты). Встроен во ВСЕ точки выдачи: `list_page_widgets`, батч `compute_page_data`, единичный `compute_widget_data` (fail closed, skip_acl для батча), `widget_drill`, xlsx-экспорт страницы. CRUD грантов стал scope-aware: `add_grant(scope,widget_id)` с проверкой принадлежности виджета дашборду + dedup через `is not distinct from`; `list_grants` отдаёт оба scope + имя виджета; `remove_grant` снимает любой; эндпоинт грантов отдаёт `widgets` (плоский список для выбора). Фронт: `GrantIn`/`DashGrant`+scope/widget_id/widget_name, `AccessEditor` — секции «Доступ к дашборду целиком» и «Доступ к отдельным виджетам», селектор «Область» (весь дашборд/виджет)→выпадашка виджетов, предупреждение о whitelist-семантике. **Автотесты `test_rls_widget.py` (2):** полный flow (грант дашборда+публикация→viewer видит оба виджета; +widget-грант на A→viewer видит только A [батч/список/данные B→404/drill B→404], админ видит оба; снятие→снова оба) + негатив (виджет чужого дашборда→отклонён). Проверено СКВОЗНО (весь пакет **61 тест зелёный**, из них 2 новых RLS) и ВЖИВУЮ (AccessEditor на «Динамика тест»: обе секции, «Область»→«Отдельный виджет» показал все 4 виджета с именем страницы; консоль чистая; грант не сохранял — 0 widget-грантов в БД). Фронт-тесты 11 зелёные, tsc/build 0. ДАЛЬШЕ по бэклогу: рассылка PDF по SMTP · комментарии к дашбордам · онбординг/тултипы · RLS на СТРОКИ данных (ABAC миграция 005 — отдельный крупный шаг) |
| 2026-07-25 | ПЕРФ — ПАГИНАЦИЯ ОСТАЛЬНЫХ СПИСКОВ (волна 1/3 из запроса пользователя; после документов/аудита закрыли все реально растущие списки). Единый серверный конверт `{total,limit,offset,items}` + серверный ПОИСК (ilike) для: **пользователи** `GET /users?q=&limit=&offset=` (по логину/ФИО), **метрики** `GET /metrics?q=&limit=&offset=` (по коду/названию; пикеры типа KPI-«Главной» тянут limit=500 и читают items), **дашборды** `GET /dashboards?q=&fav=&limit=&offset=` (поиск+фильтр избранного перенесены С КЛИЕНТА на сервер, сохранён порядок «избранные сверху», видимость через RLS). Общий тип `Page<T>` в api/http.ts. Фронт: UsersPage/MetricsPage/DashboardsPage — поиск с дебаунсом 250мс, кнопка «Показать ещё (N)», total в заголовке; HomePage-пикер метрик → limit=500+items. **Защита от гонки ответов** (useRef-счётчик «побеждает последний запрос») во всех трёх списках — иначе поздний ответ мог перезаписать свежий (замечено вживую при быстром вводе). objects/folders НЕ пагинировал (структурные, ограничены масштабом; objects ещё и питает выпадашки). Бэкенд-тесты обновлены (test_rls читает .json()["items"]). Проверено СКВОЗНО (конверты у всех трёх; поиск q=план→2 метрики, q=Динам→2 дашборда; страницы не пересекаются; limit=0→422; fav=true→0) и ВЖИВУЮ (UsersPage «Пользователи (N)» + поиск→«Ничего не найдено»; DashboardsPage поиск «Динам»→ровно 2 строки, скриншот; консоль чистая). 61 бэкенд-тест + 11 фронт зелёные, tsc/build 0. ДАЛЬШЕ волна 2/3: комментарии/лента к дашбордам; затем волна 3/3: RLS на строки данных |
| 2026-07-25 | КОЛЛАБОРАЦИЯ — КОММЕНТАРИИ/ОБСУЖДЕНИЕ К ДАШБОРДАМ (волна 2/3). Миграция 023_dashboard_comments.sql (dashboard_comments: dashboard_id CASCADE, user_id SET NULL, body, created_at; индекс (dashboard_id, created_at desc)). Модуль `dashboards/_comments.py`: `list_comments` (конверт {total,limit,offset,items}, новые сверху, флаг can_delete=автор|привилегированный), `add_comment` (RLS _can_view — писать может любой, кто видит дашборд; валидация непусто/≤4000; **уведомляет автора дашборда** через notif_svc.notify event `dashboard.comment`, если это не он сам и активен), `delete_comment` (автор либо привилегированный, иначе 400; пишет audit delete). Эндпоинты `/dashboards/{id}/comments` GET/POST + DELETE `/{comment_id}` (чтение/запись под get_current_user+RLS, НЕ manage). Подпись события в notifications EVENT_LABELS. purge_dashboard дочищает notification_events по дашборду. Фронт: api Page<DashComment>+list/add/delete; компонент `dashboards/Comments.tsx` — модалка-лента (автор·время·тело, удаление своего/для админа, textarea+Отправить, «Показать ещё», защита от гонки); кнопка «💬 Обсуждение» в тулбаре дашборда (доступна ВСЕМ зрителям, не только canManage). Автотесты `test_comments.py` (3): flow+RLS+уведомление автору; отказ без доступа (404); нельзя удалить чужой. Проверено СКВОЗНО (POST→GET total1 author/can_delete; пустой→422; чужой дашборд→404; DELETE→204→total0) и ВЖИВУЮ (панель на «Динамика тест»: публикация комментария→карточка с автором «Администратор»+время, удаление→«Пока нет комментариев», консоль чистая, скриншот). Тестовый след (комментарии/audit/уведомления) вычищен. **64 бэкенд-теста** (61+3) + 11 фронт зелёные, tsc/build 0. ДАЛЬШЕ волна 3/3: RLS на СТРОКИ данных (ABAC миграция 005) |
| 2026-07-25 | БЕЗОПАСНОСТЬ — ROW-LEVEL RLS НА СТРОКИ ДАННЫХ по подразделению (волна 3/3, крупная; согласована модель «строки по подразделению/району»). Миграция 024_data_row_acl.sql (data_row_acl: object_id CASCADE, department_id CASCADE, row_label; unique+индекс). Модель opt-in НА ОБЪЕКТ: пока для объекта нет правил — строки видят все; появилось ≥1 правило — непривилегированный видит только row_label, выданные его отделу (whitelist, пустой набор/нет отдела → fail closed, ничего); привилегированные и предпросмотр — все. **Граница:** фильтруются ВИДЖЕТНЫЕ чтения датасета (таблица, серии bar/line/pie, compare, heatmap, pivot, динамика, KPI/gauge/plan-fact по dataset_code, drill-до-строк); именованные МЕТРИКИ (формульный движок) и objects_compare НЕ фильтруются (объективные/кросс-объектные). Реализация: `dashboards/_rowrls.py` (allowed_rows_for_dataset → None|set; rls_tag для кэша; CRUD get/set_row_acl + инвалидация cache.delete_prefix('wd:')); фильтр `_row_acl_clause` встроен в 4 dataset-хелпера `_widgetdata.py`; `_compute_widget(user=)` вычисляет allowed по объекту датасета; **ключ TTL-кэша дополнен меткой подразделения** (иначе данные одного отдела попали бы другому); батч compute_page_data и drill теперь передают user. Эндпоинты GET/PUT `/objects/{id}/row-acl[/{dep}]` (manage). Фронт: api getRowAcl/setRowAcl; компонент `RowAclEditor.tsx` — модалка (отдел→чекбоксы строк объекта, предупреждение о включённом RLS, счётчик); кнопка «🔐 Доступ к строкам» в виде объекта (ObjectsPage, canManage). Автотесты `test_row_rls.py` (2): таблица+KPI (нет правил→все; отдел «Паспорт»→только Паспорт, KPI 100 vs admin 180; get_row_acl; снятие→все) + fail-closed без отдела. Проверено СКВОЗНО (**66 тестов зелёных**, +2) и ВЖИВУЮ (редактор на «Динамика тест»: отдел→чекбокс строки→сохранение→«RLS ВКЛЮЧЁН»+счётчик (1); консоль чистая, скриншот). Тестовые данные (правила/отдел) вычищены. Все 3 запрошенные волны ГОТОВЫ. tsc/build 0. ЗАМЕЧЕНО на будущее: смена row-ACL чистит ВЕСЬ префикс кэша wd: (редкое действие — ок); object-level ACL (скрыть объект целиком) и фильтрация objects_compare — отдельная тема |
| 2026-07-25 | UX — ОНБОРДИНГ-ПОДСКАЗКИ + ТУЛТИПЫ ВИДЖЕТОВ (фронт-фича, без бэкенда). **Онбординг:** компонент `OnboardingHint.tsx` — короткий гасимый баннер вверху раздела, текст зависит от роли (руководитель admin/moderator видит расширенную подсказку), закрытие запоминается по (login, section) в localStorage (`onbd:{login}:{section}`). Тексты для home/objects/metrics/dashboards/users/reports/audit. Подключён ОДНИМ местом в `App.tsx` (Shell: `<OnboardingHint section roles userKey={me.login}/>` над контентом раздела). **Тултипы виджетов:** переиспользуемый `InfoTip.tsx` (ℹ️ «i»-кнопка → hover/клик-поповер); на карточке виджета в DashboardsPage рядом с бейджем типа — `InfoTip` с текстом = подсказка типа (из `WIDGET_META` галереи) + авторская заметка `config.help` (через `widgetTip(w)`). В `WidgetForm` добавлено опциональное поле «Подсказка (тултип)» → `config.help`. Проверено ВЖИВУЮ: баннер Главной с расширением для руководителя; закрытие→исчез+флаг localStorage=1, по секциям независимо, после навигации не возвращается; баннер дашбордов; на дашборде 4 ℹ️-кнопки (по одной на виджет), клик→тултип «Крупное значение метрики или формулы» (тип KPI); консоль чистая, скриншоты. Фронт-тесты 11, tsc/build 0. Тестовых данных в БД нет (фронт). ДАЛЬШЕ (бэклог): рассылка PDF по SMTP · object-level ACL · CI Node20 |
| 2026-07-25 | ДОК + CI-РЕВИЗИЯ. По решению заказчика **рассылка PDF по SMTP** и **object-level ACL** — НЕ реализуем; зафиксированы как осознанно отложенные в новом `docs/Руководство_администратора.md` (раздел «Осознанно отложенные возможности»: суть, причина отсрочки, предусловия, заметки по реализации; PDF-рассылка блокирована отсутствием SMTP МФЦ, object-ACL признан избыточным поверх готового 3-уровневого RLS — там же отмечено, что objects_compare кросс-объектный и не под row-RLS). **CI «Node 20 экшены» — оказался уже закрыт:** все экшены в `.github/workflows/ci.yml` на актуальных мажорах (checkout@v4/setup-node@v4/setup-python@v5 → рантайм Node 20), устаревших @v1..@v3 в репо нет — менять нечего, фабриковать правку не стал. Идемпотентность миграций для CI в порядке (022–024 трек-раннером, строка «применено: 0» на повторном прогоне). ДАЛЬШЕ: тёмная тема — по факту заблокирована (нет брендбука для палитры) И крупный рефактор (729 инлайн-хексов в 27 файлах, нет CSS-переменных) → вынесено на решение пользователя |
| 2026-07-25 | UI — ДВЕ ТЕМЫ (СВЕТЛАЯ + ТЁМНАЯ) на дизайн-токенах. Решение пользователя: делаем обе темы, брендбук добавит по ходу (значения уточним, разметку — нет). **Токен-слой** `src/theme.css` (~22 CSS-переменные: bg/surface/surface-2/-3/-accent/accent-weak, text/-2/-muted/-faint, accent/-strong/on-accent, border/-strong/-faint, danger/success/warn + их bg; `:root`=светлая=текущая палитра МФЦ, `:root[data-theme=dark]`=производная тёмная). `src/theme.ts` (getStored/system/current/apply/setTheme, применяется до рендера — без мигания, дефолт = prefers-color-scheme, выбор в localStorage `dashbord_theme`); `ThemeToggle.tsx` (🌙/☀️ в шапке). **Миграция 729 инлайн-цветов → var() в 27 файлах** скриптами с ручной доводкой: `#fff` разведён по роли (фон→--surface, текст-на-акценте→--on-accent); ECharts (canvas НЕ понимает var()) — 5 файлов исключены, литералы сохранены; `EChart.tsx` получает цвет текста осей/легенды из токенов через getComputedStyle + MutationObserver на data-theme (живая смена темы). Особые случаи: SVG-превью WidgetPicker возвращены в литералы (var() в SVG-атрибуте не работает); WidgetView/ReportsPage — токенизирован только HTML-хром, chart-литералы восстановлены; KioskView намеренно оставлен тёмным-slate (ТВ-экран); Login-герой оставлен (бренд, пере-делает брендбук). Проверено ВЖИВУЮ: светлая тема ИДЕНТИЧНА прежней (миграция без визуальных изменений); тёмная целостна на Главной/Дашбордах (карточки/бейджи/таблицы) и графиках (оси/подписи читаемы, живая смена) и в Отчётах; переключатель персистентен; консоль чистая, скриншоты. Фронт-тесты 11, tsc/build 0. **Брендбук:** когда придёт — менять ТОЛЬКО значения в `theme.css` (+ Login-герой), компоненты не трогать. ОСТАЛОСЬ по желанию: смягчить splitLine графиков в тёмной; довести Login под тёмную |
| 2026-07-25 | ГРАФИКИ: ECharts переведён с Canvas на **SVG-рендерер** (вопрос про Astra Linux). Разъяснено: canvas — функция БРАУЗЕРА, а не ОС; Astra-сервер раздаёт статику/API, графики рисуются на клиенте, любой совр. браузер на Astra (Firefox ESR/Chromium/Яндекс) поддерживает и canvas, и SVG. «Переезд на CSS» отвергнут (CSS не потянет heatmap/gauge/waterfall/pivot, огромный ручной переписанный код). Выбран SVG-рендерер как оптимум для гос-он-прем: `EChart.tsx` — `CanvasRenderer`→`SVGRenderer` + `echarts.init(dom, undefined, {renderer:'svg'})`; весь код графиков без изменений; тема-осознанный текст осей (getComputedStyle) и живая смена (MutationObserver) работают так же. Плюс векторные графики = чётче при печати/PDF. **Заодно ПОЧИНЕН баг миграции темы:** экспорт PDF/PNG в `DashboardsPage` получал `html2canvas({backgroundColor:'var(--surface)'})`, а html2canvas НЕ понимает CSS-переменные → фон выгрузки был бы битым; добавлен `surfaceColor()` (резолвит --surface текущей темы через getComputedStyle). Примечание: html2canvas (растеризация DOM для PDF/PNG) сам по себе использует canvas, но тоже в браузере — на Astra работает. Проверено ВЖИВУЮ: графики теперь `<svg>` (canvas в приложении = 0), чётко отрисованы в светлой и тёмной темах (столбцы/линия/таблица), живая смена; консоль чистая, скриншоты. Фронт-тесты 11, tsc/build 0 (чанк EChart 575→584 КБ, +9 КБ — незначимо) |
| 2026-07-25 | БРЕНД — применён официальный **брендбук «Мои Документы» (МФЦ)**. Извлечён из PDF (PyMuPDF): палитра из 5 цветов — **Красный #E04E39** (фирменный акцент, «фон навигации»), Тёмно-коричневый #623B2A, Бежевый #C39367, Чёрный #2C2A29 (текст), Белый (поверхность); шрифты — заголовки Circe (коммерч.), текст **PT Sans** (OFL); брендбук прямо предусматривает вариант «на тёмно-коричневом фоне» → тёмная тема аутентична. **Реализация — благодаря токен-архитектуре сменой ЗНАЧЕНИЙ в theme.css (~90% приложения перекрасилось само):** светлая = красный акцент/тёплый белый фон/бежевые чипы; тёмная = тёмно-коричневая база #1a120e + красный #f2664e + бежевые хайлайты; добавлены --brand-brown/--brand-beige + шрифт-стек --font-body/'PT Sans'→системный фолбэк. Точечно (не токены): бренд-цвета ГРАФИКОВ (PALETTE красный/бежевый/коричневый, серии #2f5496→#e04e39, тёплый градиент heatmap) в WidgetView/ReportsPage; SVG-превью WidgetPicker → бренд; **Login забрендирован** (герой красный→тёмно-коричневый градиент, BRAND.primaryDark #a5361f, фон формы var(--bg)); фирм-шрифт в корне App/Login/ChangePassword. Проверено ВЖИВУЮ (скриншоты): Login «Мои Документы», светлая тема (красные KPI/кнопки/чипы, тёплый фон), тёмная бренд-тема (коричневый фон, красные акценты), графики теперь красные SVG; переключатель работает, консоль чистая. Фронт-тесты 11, tsc/build 0. ОСТАЛОСЬ по желанию: self-host PT Sans (.woff2, OFL) для гарантии офлайн-шрифта на Astra (сейчас — системный фолбэк); заменить эмблему «D» на реальный лого-знак «Мои Документы» (нужен ассет) |
| 2026-07-26 | ЛОГОТИП «Мои Документы» добавлен. Фирменный ЗНАК извлечён из брендбука (PyMuPDF: клип-рендер стр.24 → Pillow автокроп + бел.→прозрачн.) в `frontend/src/assets/mydocs-sign.png` (знак: коричневый загиб + бежевый + красный + белая негатив-форма). Компонент `Logo.tsx` — знак на белой скруглённой плашке (как в брендбуке знак на белом носителе; читается в ОБЕИХ темах, в тёмной — как вывеска-бейдж). Размещён где уместно: **шапка приложения** (App.tsx, вместо «D»-квадрата), **страница входа** (Login-герой, вместо эмблемы «D»), **смена пароля** (ChangePassword, шапка карточки), **фавикон вкладки** (public/favicon.png — знак на белой плашке 64×64, `<link rel=icon>` в index.html; PNG-импорты типизированы через новый src/vite-env.d.ts). Проверено ВЖИВУЮ: логотип в шапке (светлая+тёмная), на странице входа (белая плашка на красном герое), фавикон грузится (image/png ok), знак-img 492×734 complete; консоль чистая, скриншоты. Фронт-тесты 11, tsc/build 0. ОСТАЛОСЬ по желанию: self-host PT Sans (.woff2) для офлайн-Astra; при желании — полный горизонтальный лого-блок (знак+вордмарк «Мои Документы») вместо знака+текста |
| 2026-07-26 | АНАЛИЗ+ТЕСТ проекта (по повестке): бэкенд **66/66 pytest**, фронт **11/11 vitest**, **tsc 0**, живой API (login/5 дашбордов/4 объекта/4 метрики), живой UI (логин/витрина/дашборды). Env локального прогона тестов: POSTGRES_HOST=localhost PORT=55432, REDIS :6380, MINIO :9800. Замечание: контейнеры `ds_*` на :80 — ЧУЖОЙ проект `video_minipc_v162_modular`, не наш; наш dev-стек `dashbord_*` рабочий, прод-стек `dashbord_prod_*` не поднят (норма). |
| 2026-07-26 | ГРАФИЧНОСТЬ КОНСТРУКТОРА — убраны остатки «кода» (по требованию: максимум мышью, минимум текста). **(1a)** `WidgetForm.tsx`: режим «Формула» (kpi/gauge) получил переключатель **«🖱 Конструктор / ⌨ Текст»** (как в Метриках), по умолчанию ВИЗУАЛЬНЫЙ — сырая DSL-строка скрыта; в визуальном режиме показывается `FormulaBuilder` + строка «Получится формула: `…`» (read-only); в текстовом — поле ввода DSL. При редактировании существующей текстовой формулы стартует режим «Текст» (чтобы её было видно). **(1b)** Картинка-виджет: вместо ввода URL — **загрузка файла** (`input[type=file] accept=image/*`, кнопка «🖼 Выбрать файл…», превью-миниатюра + «Убрать»); файл читается в **data-URI** (FileReader, лимит **512 КБ**) — встраивается в дашборд, работает офлайн на Astra, без отдельного маршрута отдачи картинок (тег img не носит Bearer; публичный эндпоинт = лишняя дыра; в Kiosk/ТВ data-URI тоже рендерится). URL оставлен как сворачиваемый «продвинутый» вариант. **(1c)** «Свой фильтр» → поле «Строка» теперь `select` из реальных строк датасета («— все строки —» + row_labels), с фолбэком на текст, если строк нет. Проверено ВЖИВУЮ (JS-клики): toggle 🖱/⌨ переключает видимость DSL/сборщика; загрузка PNG→превью data:image/png; «Строка»=select (ИНН/Паспорт РФ/СНИЛС). Тестовых данных не создано (submit не нажимался). Консоль чистая, tsc/build 0, фронт-тесты 11. |
| 2026-07-26 | ШРИФТ — **self-host PT Sans** (.woff2, OFL) для гарантии фирменной типографики офлайн на Astra (решение пользователя «встроить»). 8 сабсетов (400/700 × cyrillic/cyrillic-ext/latin/latin-ext, ~304 КБ) из Google Fonts → `frontend/public/fonts/`; `src/fonts.css` (@font-face с unicode-range, font-display:swap, url `/fonts/…`), импорт в `main.tsx` перед theme.css; лицензия `public/fonts/OFL.txt`. Стек `--font-body` уже вёл с 'PT Sans'. **Заодно ПОЧИНЕН прод-баг nginx:** в `frontend/nginx.conf` в regex API-маршрутов НЕ было `catalog|notifications|maintenance` (были в vite-прокси) → в проде Справочники/уведомления-колокольчик/Обслуживание отдавали бы index.html вместо API; добавлены. Плюс `location /fonts/` с immutable-кэшем. Проверено ВЖИВУЮ: `document.fonts.check('16px "PT Sans"')=true`, 4 сабсета 200/304, body font = PT Sans; build копирует шрифты+OFL в dist/fonts (304 КБ). tsc/build 0. Circe (заголовки, коммерч.) НЕ встраиваем — фолбэк на PT Sans. |
| 2026-07-26 | КАРТИНКА-ВИДЖЕТ — СЖАТИЕ на клиенте (устранить раздувание data-URI в БД, по ревью). Новая утилита `frontend/src/lib/image.ts`: `fileToEmbeddableDataUri` — SVG и мелкие лого (≤48 КБ) встраивает как есть; крупные растровые даунскейлит до 512px по большей стороне (canvas) + пере-кодирует (PNG/GIF→PNG или JPEG q0.85, берётся меньший); чистые хелперы `scaledDimensions`/`dataUriBytes`. `WidgetForm` onPickImage стал async со сжатием + индикатор «⏳ Обработка…» + показ итогового размера (КБ). Лимит входного файла поднят до 12 МБ (на выходе всё равно мало). Автотест `image.test.ts` (7): пропорции/границы/оценка байт. Проверено ВЖИВУЮ: вход **8.4 МБ PNG → встроено 106 КБ JPEG** (худший случай — шум; реальные лого = единицы КБ). Фронт-тестов 18 (11+7), tsc/build 0, консоль чистая. |
| 2026-07-26 | ПРОД-NGINX — ЖИВАЯ ПРОВЕРКА фикса маршрутов (по ревью). Собран прод-образ фронта `dashbord-web:nginxtest` (в нём обновлённый nginx.conf), запущен в сети dev-стека `dashbord_default` на :8090 (порт `${WEB_PORT:-8090}` — конфликта с :80 видео-проекта нет), `api` резолвится на живой dev-API. Подтверждено: `/catalog/services`, `/notifications`, `/maintenance/status` возвращают **application/json** (401/404 от API), а НЕ text/html (index.html) — фикс работает; контроль `/some-spa-route`→text/html, `/health`→json; с токеном `/notifications` и `/catalog/services`→200 json. Тестовый контейнер+образ удалены. Прод-стек целиком не поднимали (избыточно); полный smoke — на деплой-фазе. |
| 2026-07-26 | РОЛЬ «СУПЕРАДМИНИСТРАТОР» (выше admin) — по запросу владельца. Миграция `025_superadmin.sql` (роль superadmin для всех орг, идемпотентно); `bootstrap.py` — роль в SYSTEM_ROLES + рефактор `_ensure_account(role_codes:list)`, учётка владельца `superadmin` с ролями **{superadmin, admin}** (аддитивно: superadmin-права + обычный admin-доступ без правки всех require_roles), must_change_password=true; `config.py` — SUPERADMIN_LOGIN/PASSWORD (дефолт superadmin/superadmin, в проде задать в .env.prod). **Иерархия/правила** (`users/service.py`): ранги superadmin=100/admin=50; `_can_manage` — суперадмина трогает только суперадмин, остальных и admin, и superadmin; `_set_roles` — выдавать/снимать роль superadmin может ТОЛЬКО superadmin (защита от эскалации); `_guard_last_superadmin` — нельзя оставить систему без активного суперадмина (блок/удаление/снятие роли); новое исключение `UsersForbidden`→403. **Гибридное удаление** (решение пользователя) `delete_user`: жёстко удаляет только «чистого» юзера; при связанных данных ловит `asyncpg.ForeignKeyViolationError`→400 «используйте блокировку» (сохранить аудит; почти все FK на users — RESTRICT). Роутер: доступ `require_roles("admin","superadmin")`, актор во все операции, эндпоинт `DELETE /users/{id}`. Фронт: доступ раздела + `isAdmin`/`canManage` в App.tsx включают superadmin; `UsersPage` — бейдж «Суперадминистратор» (красный), гейтинг действий по иерархии («🔒 только суперадмин» для непозволенного), кнопка «🗑 удалить», роль superadmin в редакторе видна только тому, кто вправе выдавать; `api deleteUser`; онбординг-текст обновлён. Автотесты `test_superadmin.py` (7): вход+роль, admin не трогает суперадмина (403 блок/сброс/удаление), admin не выдаёт superadmin (403 эскалация), суперадмин управляет admin, защита последнего (self-demote 400), гибрид-удаление (чистый ok / с данными 400), self-block. **Бэкенд-тестов 73** (66+7), фронт 18. Проверено СКВОЗНО (curl :8080: роли [superadmin,admin]; admin→суперадмин 403×2; эскалация 403) и ВЖИВУЮ (admin видит суперадмина с замком+бейджами; создание+гибрид-удаление чистого юзера через UI; вход superadmin форсит смену пароля). Учётка: **логин `superadmin`, врем. пароль `superadmin` → смена при 1-м входе** (пароль владелец задаёт сам, я не завершал смену). tsc/build 0, консоль чистая, тестовые данные вычищены. |
| 2026-07-26 | ДОРАБОТКА суперадмина + правки по ревью (запрос пользователя). **(1) Авто-роль admin:** при выдаче роли superadmin в `_set_roles` автоматически добавляется и admin (нет «чистого суперадмина» без обычного доступа) — тест `test_granting_superadmin_auto_adds_admin`. **(2) Defense-in-depth:** admin-only разделы `require_roles("admin")` → `("admin","superadmin")` в audit/reports/catalog/maintenance + publish (superadmin — надмножество admin). **(3) Сброс пароля любому:** подтверждено — суперадмин сбрасывает пароль ЛЮБОГО пользователя, включая администратора (забытый пароль), тест `test_superadmin_can_reset_any_password` (сброс → вход новым паролем). **(4) Роль superadmin создаёт только superadmin** — уже было (эскалация-guard), покрыто тестом. **(5) Прозрачность картинок:** `image.ts` определяет реальную альфу по пикселям (`hasTransparency`) — прозрачные PNG сохраняются как PNG (JPEG исключён), непрозрачные → меньший из JPEG/PNG. Проверено ВЖИВУЮ (dynamic import): прозрачный PNG 2.8МБ→image/png (альфа цела), непрозрачный 2.7МБ→image/jpeg 155КБ. **(6) Гибрид-удаление UX:** при отказе (есть данные) UsersPage сразу предлагает «Заблокировать вместо удаления?» (без лишних запросов). **(7) ПОЛНЫЙ прод-стек прогнан:** собран и поднят `docker-compose.prod.yml` (.env.prod врем., WEB_PORT=8091), все 25 миграций применились, **smoke.sh — ВСЕ ПРОЙДЕНЫ** (health+БД, SPA, 401); суперадмин в прод-БД `{admin,superadmin}` must_change; фикс nginx подтверждён на реальном прод-web (/catalog//notifications//maintenance→application/json); вход суперадмина в проде ок; стек погашен (down -v), .env.prod удалён. Env-переменные SUPERADMIN_LOGIN/PASSWORD добавлены в .env.prod.example + docker-compose.prod.yml. **Бэкенд-тестов 75** (73+2), фронт 18. tsc/build 0, dev-БД чистая. |
| 2026-07-27 | ПРОВЕРКА ПРОЕКТА (старт сессии по плану): бэкенд **75→78 pytest**, фронт **18 vitest**, tsc/build 0, живой e2e (admin+superadmin, ключевые эндпоинты 200), UI рендерится, консоль чистая. Dev-стек был Exited — поднят (`docker compose up -d`). Прогон правил (feedback-*): актуальны; **обновлено устаревшее `feedback_deploy_automation`** (снял «фронт-образ+прокси нет / миграции вручную» — по факту ЕСТЬ: frontend/Dockerfile+nginx.conf+web-сервис прод-compose, migrate-сервис авто-миграций, healthcheck, smoke, backup/restore, bundle-images; осталось: HTTPS в LAN, наблюдаемость, авто-генерация секретов). Сверка памяти — консистентна (суперадмин done, PT Sans done). |
| 2026-07-27 | АВТОМАТИЗАЦИЯ — МАСТЕР ПЕРВИЧНОЙ НАСТРОЙКИ (UI-визард; выбор пользователя из повестки). **Бэкенд:** `GET /system/setup-status` (system/router.py, `require_roles("admin","superadmin")`) — счётчики по орг {departments, users, objects, documents, datasets, dashboards} + `fresh_install` (нет отделов/объектов/дашбордов и users≤2). **Фронт:** `api/system.ts` `getSetupStatus`; компонент `SetupWizard.tsx` — пошаговый модал (Приветствие с чек-листом «Готовность n/5» → Отделы → Пользователи → Первый объект → Готово), прогресс-бар, инлайн-создание (createDepartment/createUser/createObject) с живым обновлением статуса, переходы в разделы для тяжёлых шагов (загрузка документа, сборка дашборда), кнопки Назад/Далее/Пропустить/Завершить. Показывается АВТО на свежей установке админу (если не закрывал — localStorage `setup_done:{login}`), открыть вручную — кнопка «🧭 Настройка» в шапке (admin/superadmin). В редакторе пользователей мастера роль superadmin не предлагается. Автотесты `test_setup.py` (3): форма ответа для admin, 401 без токена, 403 для обычного user. **Бэкенд-тестов 78** (75+3), фронт 18. Проверено СКВОЗНО (setup-status: admin 200 с ключами, 401 без токена, user 403) и ВЖИВУЮ (лаунчер🧭 открывает мастер; чек-лист «Готовность 3/5» с реальными статусами; инлайн-создание отдела→чип появился; консоль чистая). Тестовые данные (отдел, юзер) вычищены. tsc/build 0. ДАЛЬШЕ: панель здоровья + автопочинка прод; деплой-фаза (HTTPS/наблюдаемость). |
| 2026-07-27 | МАСТЕР НАСТРОЙКИ — устранены слабые места по ревью (доведён до завершения настройки ЦЕЛИКОМ внутри мастера). **(1) Инлайн-загрузка документа** (шаг «Данные»): выбор объекта + папки (или создание папки инлайн `createFolder`), файл + отчётная дата → `uploadDocument` — без ухода в раздел. **(2) Инлайн-сборка дашборда** (шаг «Дашборд»): выбор объекта → «✨ Собрать из объекта» (`autoBuildDashboard`, переиспользует авто-сборку) или пустой по названию (`createDashboard`). Шаги мастера: Приветствие→Отделы→Пользователи→Объект→Данные→Дашборд→Готово (7). **(3) Серверный флаг завершения** вместо localStorage: миграция `026_org_setup.sql` (organizations.setup_dismissed), `POST /system/setup-dismiss` (admin/superadmin, 204), поле `setup_dismissed` в setup-status; закрытие/пропуск/навигация ставят флаг → мастер не всплывает при СМЕНЕ БРАУЗЕРА. **(4) Надёжный `fresh_install`** — по СТРУКТУРНОЙ пустоте (нет отделов/объектов/дашбордов), убран хрупкий `users≤2`. App.tsx авто-показ = `fresh_install && !setup_dismissed`. Автотесты `test_setup.py` +2 (dismiss ставит флаг 204; user→403), итого 5. **Бэкенд-тестов 80** (78+2), фронт 18. Проверено ВЖИВУЮ: шаг «Данные» (выбор реального объекта/папки, инлайн-создание папки `ztest_папка`→появилась, файл+дата), шаг «Дашборд» (объект+«Собрать из объекта»), «Пропустить»→setup_dismissed=true (серверный). Тестовые данные (папка, флаг) вычищены. tsc/build 0. ДАЛЬШЕ: панель здоровья + автопочинка прод. |
| 2026-07-27 | АВТОМАТИЗАЦИЯ — ПАНЕЛЬ ЗДОРОВЬЯ + АВТОПОЧИНКА (админ/суперадмин). Панель во многом УЖЕ была (`/reports/system`: psutil CPU/RAM/диск/uptime/load + db_size + статусы PostgreSQL/Redis/MinIO) — НЕ дублировал, а ДОПОЛНИЛ: **латентность каждого сервиса** (latency_ms) + **общий `status`** (ok/degraded: любой сервис вниз или ресурс в danger). **Новое — АВТОПОЧИНКА:** `maintenance/service.py heal()` — безопасные идемпотентные восстановления уровня приложения: (1) MinIO бакет — создать, если пропал (частая проблема после сброса тома); (2) Redis — проверить связь. Возвращает список действий с итогом; эндпоинт `POST /maintenance/heal` (admin/superadmin). Инфра-авто-рестарт упавших контейнеров — Docker `restart: unless-stopped` (уже в compose), в UI это честно указано. **Фронт:** `api/maintenance.ts healSystem` + `HealResult`; SystemReport дополнен status/latency_ms; ReportsPage — секция переименована в «Здоровье системы», бейдж статуса (✓ в норме / ⚠ проблемы), латентность на чипах сервисов, кнопка «🔧 Починить» → показ результата, гейт+superadmin. Автотесты `test_health.py` (3): status+латентность в отчёте, heal возвращает действия (admin), heal 403 обычному user. **Бэкенд-тестов 83** (80+3), фронт 18. Проверено СКВОЗНО (reports/system status ok + латентность PG1.2/Redis2.1/MinIO1.5 мс; heal→бакет «уже был»/Redis «доступен» healthy) и ВЖИВУЮ (панель: статус, гейджи, аптайм/размер БД, сервисы с мс; «Починить»→«Результат починки ✓»). tsc/build 0 (устаревшие ошибки в консоли — транзиентный кэш HMR от правки, build чист). ДАЛЬШЕ: деплой-фаза (HTTPS в LAN, наблюдаемость, авто-генерация секретов, проверка офлайн-бандла). |
| 2026-07-27 | АУДИТ РИСКОВ + ПОЧИНКА 3 слабых мест (по запросу пользователя). Аудит: SQL безопасен (плейсхолдеры $N + params, `.format(n=…)` только индекс), XSS нет (нет dangerouslySetInnerHTML, React экранирует, SVG в `<img src>` безопасен), CORS не нужен (same-origin через nginx), пароли bcrypt, JWT в проде обязателен (:? в compose), логин не утекает существование юзера + lockout, смена пароля валидируется. Публичны только /health,/,/auth/password-policy,/system/info (намеренно). **Починено:** **(#1 СРЕДНИЙ) `must_change_password` теперь форсится на БЭКЕНДЕ** — `get_current_user(request)` при временном пароле пускает только к /auth/me и /auth/change-password, остальное → 403 «Сначала смените временный пароль» (раньше фронт форсил экран, но прямой API-запрос обходил). Conftest сбрасывает флаг у admin/superadmin (сид-учётки заводятся с must_change=true; для тестов «онбордены»). Тест test_must_change_password_blocks_api. **(#2) Серверный лимит размера загрузки документа** — `_read_capped` читает чанками по 1 МБ с ранним обрывом → HTTP 413 при >50 МБ (ограничивает память даже без nginx-прокси; MAX_UPLOAD_BYTES=50 МБ). **(#3) Синхронные MinIO-вызовы в async → asyncio.to_thread** в `maintenance.heal` (ensure bucket) и `reports.system_health` (bucket_exists) — не блокируют event-loop при зависшем/недоступном MinIO. Мелочи оставлены осознанно (/system/info info-disclosure незначим, токен в localStorage — стандарт SPA без XSS, нет общего rate-limit — LAN on-prem + lockout входа, диск-метрика приближение). **Бэкенд-тестов 84** (83+1), фронт 18. Проверено СКВОЗНО (must_change: 403 до смены/200 после; heal+system async-MinIO ok; малый upload 201) — консоль чистая, tsc 0, тестовые данные вычищены. Готово к деплой-фазе. |
| 2026-07-27 | ДЕПЛОЙ-ФАЗА (4 пункта, по запросу). **(1) Авто-генерация секретов:** `gen-secrets.sh` — .env.prod с сильными случайными секретами (openssl rand: POSTGRES/MINIO/JWT + временные admin/superadmin пароли, печатаются 1 раз), права 600, идемпотентно; deploy.sh авто-генерирует если нет .env.prod. **(2) HTTPS в LAN:** `gen-tls.sh` (самоподписанный сертификат в certs/, SAN localhost+опц.hostname/IP, 825 дн); маршруты nginx вынесены в партиал `nginx.locations.conf` (include — единый список для HTTP и HTTPS, устраняет дрейф); `nginx.tls.conf` (80→301 https + 443 ssl TLSv1.2/1.3 + HSTS); Dockerfile копирует партиал+tls, EXPOSE 80 443; `docker-compose.tls.yml` (оверлей: сертификаты+tls-конфиг+443); `deploy.sh --tls`; smoke.sh поддерживает https (curl -k). certs/+.env.prod в gitignore. Проверено ВЖИВУЮ: HTTPS-стек собран+запущен — /health JSON, SPA, /dashboards 401, HTTP→301, smoke https ВСЕ ПРОЙДЕНЫ. **(3) Наблюдаемость (Prometheus/Grafana/Loki):** `app/observability.py` — PrometheusMiddleware (http_requests_total + http_request_duration_seconds по ШАБЛОНУ маршрута) + экспозиция `/internal/metrics` (не /metrics — занят формулами); prometheus-client в requirements. `docker-compose.monitoring.yml` (пины): prometheus (скрейп api/node/cadvisor) + grafana (провижининг datasources Prometheus+Loki + стартовый дашборд rps/p95/5xx/логи) + loki + promtail (docker sd) + node-exporter + cadvisor; конфиги в monitoring/*; запуск после осн.стека (external сеть). Проверено ВЖИВУЮ: /internal/metrics Prometheus-формат (ASGI+живой контейнер после пересборки dev-api); compose config ✓; promtool SUCCESS; **Prometheus на dev-сети → таргет dashbord-api UP, http_requests_total в Prometheus**. Тест test_prometheus_metrics_exposed. **(4) Офлайн-бандл:** `bundle-images.sh` + пост-проверка целостности (list манифеста+размер). Проверено ЧАСТИЧНО: buildx amd64 доступен, pull amd64-баз (pg/redis/minio) ✓, механизм docker save→tar→load ✓ (нативный образ 22М, база 221М). Полная amd64-сборка под QEMU-эмуляцией на arm64-Mac не уложилась в бюджет времени; объединённый `docker save` нескольких кросс-платформенных образов упёрся в quirk containerd-store Docker Desktop **на arm64** (на нативном amd64-сервере Astra — цель поставки — его нет). **Бэкенд-тестов 85** (84+1), фронт 18, tsc/build 0. Всё запушено. ДАЛЬШЕ: на реальном amd64-сервере прогнать полный `./bundle-images.sh` + `./deploy.sh --tls` end-to-end; при желании — алерты Grafana/Alertmanager, Keycloak (SSO/MFA — ранее отложено). |
| 2026-07-27 | ДЕПЛОЙ END-TO-END ПРОГОН + фиксы. **`./deploy.sh --tls` прогнан ПОЛНОСТЬЮ вживую (arm64-локально, Docker собирает/запускает нативно):** авто-gen-secrets (.env.prod без CHANGE_ME) → gen-tls → сборка образов → полный прод-стек (pg/redis/minio healthy, migrate ok, api healthy, worker, web) → **smoke HTTPS 8443 ВСЕ ПРОЙДЕНЫ**; прод-api отдаёт `/internal/metrics` (http_requests_total — образ с prometheus-client), HTTPS 443→200; стек погашен down -v, .env.prod+certs очищены. **Фикс:** `gen-secrets.sh` теперь генерит и GRAFANA_PASSWORD (иначе deploy.sh падал бы на проверке CHANGE_ME из-за добавленного позже ключа). **`bundle-images.sh` сделан устойчивее:** сохранение образов ПО ОДНОМУ (объединённый docker save падает на containerd-store Docker Desktop) → упаковка в один tar + автозагрузчик load.sh + проверка целостности (n_tar==n_img, иначе exit 1). Проверено: упаковка/загрузка работает (4/5 образов roundtrip: api/web/postgres/minio ✓, load из бандла ✓); `redis:7-alpine` не сохранился — **containerd-quirk Docker Desktop на arm64** (digest не материализован), на нативном amd64-Astra отсутствует. **ИТОГ деплой-фазы:** секреты/HTTPS/наблюдаемость — проверены end-to-end; полный офлайн-бандл (все 5 образов) собирается на нативном amd64-сервере Astra (единственное, что нельзя завершить на arm64-Mac из-за Docker Desktop). Бэкенд-тестов 85, фронт 18. **⏰ НАПОМИНАНИЕ (опционально, НЕ реализовано — по решению пользователя оставить в доках):** (а) алерты Grafana/Alertmanager (правила на p95/5xx/диск); (б) Keycloak (SSO/MFA) — ранее отложено, при возврате объяснить, как работает. |
| 2026-07-27 | ЗАКРЫТЫ 3 оставшихся мелких пункта §7 (по запросу). **(1) Значок свежести на виджете:** `_widgetdata._dataset_as_of` (дата активного выпуска датасета) + хелпер `_attach_as_of` → поле `as_of` в данных ДАТАСЕТНЫХ виджетов (метрики/текст — без него, объективны); подключён в `compute_widget_data` (просмотр+батч) и `preview_widget` (конструктор). Фронт: `WidgetView` — подвал «🕓 данные на ДД.ММ.ГГГГ» (fmtAsOf). Тест test_freshness_as_of. **(2) Перепривязка шаблона при клонировании:** `service._template_codes` (какие коды датасетов/метрик использует шаблон), `_remap_config` (старый код→новый в dataset_code/metric_code/plan_metric/fact_metric), `template_bindings`; `create_from_template(dataset_map, metric_map)`. Эндпоинт `GET /dashboard-templates/{id}/bindings`; `InstantiateIn` принимает dataset_map/metric_map. Фронт: `createFromTemplate` проверяет наличие всех кодов в текущих источниках (getTemplateBindings + getDataSources); если каких-то нет — открывает **RebindModal** (⚠ отсутствующие подсвечены, для каждого выбор замены из доступных, кнопка активна только когда все ⚠ сопоставлены); совпадающие можно не трогать. Тесты test_templates.py (3: извлечение кодов, remap, bindings+instantiate). **(3) Cross-filter расширенный:** выяснено, что «разные датасеты» УЖЕ связываются (catOptions объединяет строки всех датасетов страницы → клик фильтрует все датасетные виджеты по общей строке). Добавлено: клик-**toggle** (повторный клик по строке снимает фильтр) + заметный **чип «🔗 связано: X ✕»** в панели фильтров (видно, что вся страница связана; ✕ снимает). Именованные МЕТРИКИ осознанно НЕ участвуют — объективные агрегаты (SUM по всем строкам); фильтр по одной строке исказил бы KPI. Проверено ВЖИВУЮ: бейдж «данные на 01.03.2026»; чип «связано: Паспорт»→✕ снимает; консоль (устаревшие HMR-ошибки от прошлой правки, build чист). **Бэкенд-тестов 89** (85+4), фронт 18, tsc/build 0. **§7 ПОЛНОСТЬЮ ЗАКРЫТ.** |
| 2026-07-27 | ДЕПЛОЙ «ОДНОЙ КОМАНДОЙ» — `install.sh` + инструкция. Главное правило пользователя: архив копируется на Astra → разархивируется → разворачивается ПОЛНОСТЬЮ АВТОМАТИЧЕСКИ минимумом команд. **`install.sh`** (единый автоустановщик): (1) проверяет Docker + docker compose v2 + демон (сам НЕ ставит — шаг ops); (2) если рядом `dashbord-images.tar` — распаковывает и `docker load` (офлайн, через load.sh из бандла) → деплой `--no-build`; иначе сборка из исходника; (3) вызывает `deploy.sh` (секреты→TLS→миграции→запуск→smoke). Флаги: `--no-tls`, проброс прочих; сертификат под адрес — `TLS_CN=/TLS_SAN= ./install.sh`. Проверено ВЖИВУЮ: `./install.sh` целиком развернул прод-стек (сборка→секреты→TLS→миграции→запуск→**smoke HTTPS ВСЕ ПРОЙДЕНЫ**), затем погашен. Инструкция `docs/Развертывание_на_сервере.md` (предпосылки, быстрый старт одной командой, 2 сценария [онлайн/офлайн-бандл], сертификат под сервер, после установки [вход superadmin/мастер], наблюдаемость, бэкап/обновление/диагностика, шпаргалка порядка действий, безопасность). Архивы Final_v.1.*/dashbord-images.tar → в .gitignore (артефакты). Итог: развёртывание на Astra = `tar -xf … && cd Dashbord && ./install.sh`. |
| 2026-07-27 | БЛОК А НАДЁЖНОСТИ (7 пунктов ревизии, согласовано) + виджет «ГОД К ГОДУ». **(1) Ротация docker-логов** — `x-logging` (json-file 10m×3) во ВСЕХ compose (dev/prod/monitoring), все сервисы; `docker compose config` ✓. **(2) gzip + security-заголовки** — nginx.locations.conf (общий HTTP/HTTPS): gzip on (js/css/json/svg, comp 5, proxied any) + X-Content-Type-Options/X-Frame-Options/Referrer-Policy/CSP (default-src 'self'; img data:+blob: для картинок-виджетов/экспорта; style unsafe-inline для React) — с ДУБЛЕМ в /assets/ и /fonts/ (add_header в location отключает наследование!). **(3) Мониторинг офлайн** — bundle-images.sh теперь кладёт и 6 образов наблюдаемости (пины из .env.prod, флаг --no-monitoring), `install.sh --monitoring` поднимает Prometheus/Grafana/Loki сразу после деплоя. **(4) Автобэкап из install.sh** — по умолчанию ставит расписание (root/sudo -n, иначе печатает команду), флаг --no-backup. **(5) Healthcheck worker + mem_limit** — WorkerSettings.health_check_interval=60 + `arq --check` healthcheck (проверен вживую exit 0); mem_limit всем прод-сервисам (pg/api/worker 1g, minio 512m, redis 256m, web 128m). **(6) Rate-limit /auth/login** — limit_req_zone 10r/m burst 5 в nginx.conf+nginx.tls.conf, `location = /auth/login` в общем конфиге; вживую: 6 прошло → далее 429, /health не ограничен. **(7) Восстановимость бэкапа** — backup.sh: pg_restore --list + tar -tzf сразу после снятия (битый дамп ловится в момент бэкапа); BACKUP_DIR в .env.prod.example с рекомендацией отдельного диска. + Убран pytest-warning (test_templates: file-level asyncio-марк → только на async-тест). **ВИДЖЕТ «ГОД К ГОДУ» (yoy):** бэкенд — тип в WIDGET_TYPES, расчёт в _widgetdata (помесячные ряды последнего года данных против предыдущего из период-серий датасета; сравнение по СОПОСТАВИМЫМ месяцам — неполный год не сравнивается с полным; row-RLS/cross-filter «Строка» применяются, глобальный период — нет [осознанно]; ветка xlsx-экспорта); фронт — карточка в пикере (группа «Графики», svg-превью пунктир+линия), WidgetForm (датасет+поле, дефолт-конфиг), WidgetView (ECharts: прошлый год пунктир беж, текущий сплошной красный, легенда годов, подпись «К YYYY г. (сопоставимые месяцы: N): ↑ +Δ (%)»). Найден и исправлен баг первой версии (KeyError: RHS присваивания вычисляется до setdefault). Тесты: test_yoy (бэкенд, 2 года: 165 vs 100 за январь → +65/65%), пикер-тест дополнен. Проверено СКВОЗНО (preview позитив 1 год/2 года, негативы 400) и ВЖИВУЮ в браузере (галерея→форма→живой предпросмотр 1 год [честная подпись «нет данных прошлого года»] → врем. 2025-выпуски → две линии + «↑ +50 (29.41%)» [(100+120)−(80+90)=50 ✓] → виджет добавлен на страницу → удалён; временные выпуски удалены, БД чистая; консоль чистая). Прод-web-образ прогнан в тест-контейнере: заголовки/gzip (Content-Encoding: gzip на JS)/429 подтверждены curl'ом, nginx -t TLS-конфига ✓, тестовые контейнер/образ удалены. **Бэкенд-тестов 90** (89+1), фронт 18, tsc/build 0. ОСТАЛОСЬ ИЗ РЕВИЗИИ: прогон bundle-images.sh + install.sh на amd64-Astra (организационно, п.8); блок В по желанию (алерты по метрикам, PDF-рассылка ждёт SMTP). |
| 2026-07-27 | **П.8 ЗАКРЫТ БЕЗ ASTRA — репетиция на нативном amd64 (GitHub Actions) + 3 РЕАЛЬНЫХ ФИКСА.** Решение пользователя: блок В НЕ делаем; вопрос «как проверить amd64 без Astra?» → новый ручной workflow `.github/workflows/amd64-install-check.yml` (workflow_dispatch; файл также на main — требование GitHub): amd64-раннер → `bundle-images.sh` → проверка, что все образы в бандле amd64 → удаление локальных образов (имитация чистого офлайн-сервера) → `install.sh --monitoring` (загрузка из tar → секреты → TLS → миграции → smoke) → пост-проверки → бэкап. **Прогон №5 ПОЛНОСТЬЮ ЗЕЛЁНЫЙ** (run 30207569084): 11/11 образов amd64; стек healthy; worker healthy (arq --check); rate-limit подтверждён (5×401 → 429); gzip (content-encoding: gzip на JS); CSP/nosniff; Prometheus ready + Grafana ready (--monitoring, офлайн-образы из бандла); backup.sh с проверкой восстановимости + systemd-таймер от пользователя. **Прогоны №1–4 нашли и починены 3 РЕАЛЬНЫХ бага, которые ударили бы на Astra:** (1) `env_get` в bundle-images.sh/backup.sh: grep по отсутствующему .env.prod → код 2 → pipefail+set -e молча убивал скрипт на чистой машине (fix `|| true`, как в backup-schedule); (2) ГОНКА в deploy.sh: smoke мог стартовать раньше bind'а nginx (на раннере web поднялся за 40мс до smoke) → теперь deploy ждёт ответа веб-прокси до 30с перед smoke; (3) автобэкап через sudo создавал backups/ root'ом → последующий ручной `./backup.sh` админа падал на mkdir → теперь systemd-unit `User=$SUDO_USER`, cron в crontab пользователя, chown каталога, ротация не падает на чужих наборах. +CI-шаг: retry для Grafana (стартует >1с). Не покрыто (только реальная Astra): версия Docker из репо Astra, офлайн-apt, старый CPU (закрыт пином MinIO). Docs: Развертывание_на_сервере.md — раздел «Репетиция без Astra». |
| 2026-07-27 | **КОМПЛЕКТ ДОКУМЕНТАЦИИ .DOCX С ИЛЛЮСТРАЦИЯМИ (по запросу) + пересборка архивов.** Сняты 25 РЕАЛЬНЫХ скриншотов UI (Playwright+Chromium, retina 2x, три роли: admin полный обход всех разделов; временные ztest_mod/ztest_user — интерфейс модератора и пользователя, экран обязательной смены пароля, read-only просмотр по гранту; учётки и события входа вычищены из БД после съёмки). Собраны 5 документов в docs/ (docx-js, фирменный стиль: PT Sans, брендовый красный, титул+оглавление+подписи «Рис. N», таблицы, врезки): **Паспорт_системы.docx** (назначение/архитектура/функции/RLS/безопасность/качество), **Установка_и_настройка.docx** (одна команда, онлайн/офлайн-сценарии, TLS, мониторинг, бэкап+восстановление, обновление, диагностика, справочник .env.prod, шпаргалка Astra), **Руководство_администратора.docx** (роли+3 уровня RLS, мастер, пользователи/суперадмин/журнал входов, объекты/ретенция/свежесть, доступы, модерация, справочники, аудит, здоровье+автопочинка, отложенные функции), **Руководство_модератора.docx** (конвейер данных, визуальный конструктор формул+DSL, конструктор дашбордов: галерея 16 виджетов/фильтры/cross-filter/алерты/шаблоны, публикация, доступы, памятка), **Руководство_пользователя.docx** (вход/смена пароля, интерфейс, просмотр/фильтры/прозрачность, экспорт, FAQ). Каждый docx проверен рендером (docx-preview в Chromium, постранично отсмотрен). Тех-находка: незаспредленный массив в children docx-js даёт битый XML (`<0/>`) — в doclib.js добавлен children.flat(Infinity). Архивы Final_v.1.tar/.zip пересобраны из свежего HEAD (git archive: все фиксы блока А + amd64-фиксы + docx-документация). |
| 2026-07-27 | **ТРЕТЬЯ ТЕМА ОФОРМЛЕНИЯ «МинЭк ДНР»** (по запросу; скилл minek-dnr, Brand Book 2026 Минэкономразвития ДНР). theme.css: блок `[data-theme="minek"]` — белый/off-white #F4F4F2 фоны данных, золото #B88C46 как акцент (градации #E0BB72/#8F6A2E), титульный тёмный #0E1730, текст #4A5168, хэйрлайны #CFCFCF, синяя плашка #1B285C как вторичный цвет графики; шрифтовый стек Golos Text/Oswald с фолбэком на PT Sans (self-host шрифтов МинЭк — опционально позже). **ВАЖНОЕ УЛУЧШЕНИЕ АРХИТЕКТУРЫ: палитра серий графиков вынесена в CSS-токены** `--chart-1..7`, `--chart-prev`, `--chart-heat-1..5` (заданы во всех трёх темах) — WidgetView больше не содержит жёстких цветов: chartColors() читает токены через getComputedStyle, хук useThemeVersion() перерисовывает графики при смене data-theme (проверено вживую: линия графика мгновенно золото↔красный без перезагрузки). ThemeToggle — переключение по кругу светлая→тёмная→МинЭк (иконка следующей темы, 🏛️ для МинЭк). Логин-страница осознанно остаётся в бренде МФЦ. Docx обновлены XML-правкой (руководства пользователя/администратора + паспорт: «три темы»); ТЕХ-ЗАМЕТКА: scratchpad-генераторы docx и скриншоты удалены системной очисткой temp — при крупной перегенерации доков воссоздать скрипты заново. Проверено вживую (логин/главная/дашборд в МинЭк, живой цикл переключения, консоль чистая); tsc 0, vitest 18, build 0. |
| 2026-07-27 | **АРХИВ ДАШБОРДОВ (идея пользователя, все 6 предложений согласованы).** Ключевой принцип — СЛЕПОК: при архивации сохраняются РАССЧИТАННЫЕ данные всех виджетов (jsonb), архив показывает «как было», даже если данные позже удалены ретенцией; слепок объективен (без row-RLS/фильтров) — поэтому доступ в раздел избирательный. **Миграция 027**: dashboard_archive (слепок+тема+note+archive_month YYYY-MM+prev_status+auto+архиватор, dashboard_id ON DELETE SET NULL — слепок переживает дашборд), archive_access (допуски), dashboards.auto_archive, +enum audit_action archive/unarchive. **Бэкенд** dashboards/_archive.py: can_view_archive (привилегированные всегда; user — по допуску; roles дочитываются из БД — get_current_user их не кладёт), _build_snapshot, archive_dashboard (слепок→статус archived→из списка; повтор→400), unarchive (восстанавливает prev_status, слепок остаётся), delete_archive (только admin), months/topics/list (поиск ilike имя+тема, фильтры месяц/тема)/get, snapshot_to_xlsx (Excel из ЗАМОРОЖЕННЫХ данных, все 14 дата-типов), run_monthly_auto_archive (слепки за ПРОШЛЫЙ месяц, идемпотентно), доступы list/add/remove. Роуты: /dashboards/{id}/archive|auto-archive, /archive (me/months/topics/list/{id}/export.xlsx/unarchive/DELETE), /archive-access; АУДИТ всех действий (archive/unarchive/grant/revoke/delete). Планировщик arq: cron 1-го числа 02:00. list_dashboards исключает archived. **Фронт**: раздел «Архив» в меню (привилегированным всегда; юзеру — только при допуске, GET /archive/me), ArchivePage (месячные папки слева с количеством, поиск по названию/теме с дебаунсом, фильтр тем, карточки [тема-чип, 📅 авто-бейдж, кто/когда, страниц], просмотр слепка: плашка «Архивная копия от … данные зафиксированы», вкладки страниц, сетка 12 колонок, WidgetView batched/injData — рендер из замороженного JSON, ⤓ Excel), диалог «🔑 Доступ к архиву» (выдать/отозвать, с пояснением), в конструкторе: «📦 В архив» (диалог: тема с datalist-автоподсказкой + комментарий) и тумблер «📅 автослепок вкл/выкл»; onboarding-подсказка раздела. vite proxy + nginx locations: +archive|archive-access. **Найдено по пути**: audit_action — ENUM в БД (не только питон-whitelist) — расширен миграцией; get_current_user не наполняет roles. Тесты test_archive.py (3: полный цикл со слепком 180/xlsx PK/unarchive draft; гейтинг допусков 403→допуск→200→отзыв + запреты зрителю; автоархив n1≥1/n2=0 идемпотентно + дашборд остаётся в списке). Проверено СКВОЗНО (curl: весь цикл, права, аудит 5 событий) и ВЖИВУЮ (браузер: меню «Архив», диалог архивации с темой, месячная папка «Июль 2026», карточка, слепок KPI 140 из замороженных данных, консоль чистая; тестовые данные вычищены). **Бэкенд-тестов 93** (90+3), фронт 18, tsc/build 0. |

---

## 7. Бэклог доработок дашбордов (2026-07-12) — напоминание, делать позже

Зафиксировано по итогам архитектурного ревью расширенного блока дашбордов.
Порядок реализации (рекомендованный): П1 → сверху вниз.

### Слабые места / риски (найдены при самопроверке)
1. Нельзя ОТРЕДАКТИРОВАТЬ привязку виджета после создания (сменить датасет/поле/тип) — только удалить и создать заново. Пробел UX.
2. Доступ к дашбордам НЕ разграничен — видны всей организации. В схеме есть тонкие права (`securable_objects/object_acl/access_grants`), но для дашбордов не подключены. Нарушает требование RLS «каждый видит только разрешённое».
3. Шаблон копирует коды датасетов/метрик как есть — если в новом контексте их нет, виджеты дадут ошибку. Нужен шаг «перепривязки».
4. Cross-filter упрощён — фильтр по названию строки одного датасета; метрические виджеты и разные датасеты не связываются.
5. Графики фиксированной высоты в grid — при сильном уменьшении плитки контент подрезается (нужна адаптивная высота ECharts под ячейку).
6. Данные виджетов считаются по одному (N запросов на страницу) без кэша — лучше батч-эндпоинт «данные всей страницы» + короткий TTL-кэш.
7. Публикация не привязана к модерации — любой модератор публикует свой дашборд (правило «в эфир после approve» не enforced; стыкуется с этапом модерации).

### Что добавить (по ценности)
**Приоритет 1 — закрыть требования:**
- ✅ **KPI-алерты + условное форматирование** (СДЕЛАНО 2026-07-17) — пороги на показатель в `config` виджета (danger/warn/good, операторы сравнения, диапазоны), подсветка виджета (рамка/фон/бейдж/цвет числа), блок «⚠ Требуют внимания» на «Главной». Редактор порогов — модалка «⚠ Пороги» в конструкторе. См. журнал.
- ✅ **Разграничение доступа к дашбордам (RLS)** (СДЕЛАНО 2026-07-17) — подключён существующий ACL `access_grants` (scope=dashboard): admin/moderator/senior_moderator видят всё, остальные — по гранту на роль/пользователя + свои. Фильтрация списка/чтения/виджетов/данных/drill/версий/алертов/каталога «Главной». UI «🔒 Доступ» (выдать/убрать по роли/пользователю). См. журнал. Осталось на будущее: widget-level whitelist.
- ✅ **Глобальные фильтры дашборда + пресеты** (СДЕЛАНО 2026-07-17) — панель фильтров страницы (период + категория/строка), сохранённые наборы (миграция 015 `dashboard_filter_presets`, применить/сохранить/удалить). Осталось на будущее: виджетные фильтры (переопределение глобальных) и фильтр по объекту. См. журнал.

**Приоритет 2 — эргономика и вывод:**
- ✅ **Редактирование виджета** (СДЕЛАНО 2026-07-17) — сменить данные/тип уже созданного виджета (модалка «✎», переиспользует форму создания, сохраняет размещение и алерты).
- ✅ **Виджеты-аннотации** (СДЕЛАНО 2026-07-18) — типы `text` (заголовок/текст/выравнивание) и `image` (картинка/лого + подпись) без данных, для читаемых дашбордов (пожелание 2).
- ✅ **Режим-витрина / киоск** (СДЕЛАНО 2026-07-18) — полноэкранный `KioskView` с автопрокруткой страниц, паузой, навигацией, часами, авто-обновлением; кнопка «📺 Витрина». Для ТВ в холле МФЦ.
- ✅ **Экспорт в Excel/PNG** (СДЕЛАНО 2026-07-18) — Excel на бэкенде (openpyxl, лист-сводка + лист на виджет), PNG клиентский (снимок страницы). Кнопки PDF/Excel/PNG. **Приоритет 2 §7 закрыт.**

**Приоритет 3 — аналитика и надёжность:**
- **Мультисерии по объектам/периодам** (сравнить несколько МФЦ или план/факт/прошлый год).
- **Батч-эндпоинт данных страницы + кэш** (производительность).
- **Значок свежести данных** на виджете («данные на дату X»).
- **Планировщик рассылки PDF** и **избранное/недавние** (позже).

### Рекомендованная связка первой волны — ✅ ЗАКРЫТА (2026-07-17)
KPI-алерты + условное форматирование ✅ → доступ к дашбордам (RLS) ✅ → глобальные фильтры + пресеты ✅.
Дальше — приоритет 2 §7 (эргономика): редактирование привязки виджета, виджеты-аннотации, режим-витрина/киоск, экспорт Excel/PNG.

### Рабочее правило (задано заказчиком)
После каждой доработки — проф. архитектурное ревью + проверка работоспособности ПО ХОДУ (e2e+браузер). Система качественная/информативная/многофункциональная, но НЕ перегруженная; главный критерий — удобство для администратора, модератора, аудита и обычного пользователя.

---

## 8. Дорожная карта после волны A (утв. заказчиком 2026-07-18)

Утверждено почти всё предложенное. Все прежние правила действуют, добавлены новые
(автоматизация деплоя — [[feedback_deploy_automation]]). Порядок реализации выбран
заказчиком; «этап Деплой» — ПОСЛЕ волны B (решение заказчика 2026-07-18).

### Волна B — админ/модерация/аудит/отчёты (в выбранном порядке)
1. **Пользователи + отделы** (фундамент). Справочник `departments`; CRUD пользователей
   (ФИО раздельно, отдел из справочника, роли, блокировка, сброс пароля); раздел
   «Пользователи» в UI. Сейчас пользователи заводятся только через SQL.
2. **Аудит входов.** Таблица `login_events` (user/ip/agent/успех/время), запись в
   `/auth/login`; отчёт «кто/когда/IP/сколько раз».
3. **Отчёты** (раздел «Отчёты», сейчас пуст):
   - посещаемость/аудит входов; аудит действий (`audit_log`); качество данных
     (свежесть/устаревшие/пропущенные периоды/ошибки метрик); модерация (очередь/
     время approve/отклонённые); бизнес-сводки (KPI план/факт + алерты за период);
   - **системный мониторинг** — CPU/RAM/диск/сеть хоста, статусы сервисов
     (API/PG/Redis/MinIO/worker healthcheck), место в БД/MinIO, длина очереди воркера,
     время ответа API; ПОРОГИ + алерты; сбор `psutil`, on-prem, без внешних SaaS.
4. **Модерация дашбордов.** Очередь «на проверку» → approve → публикация («в эфир
   после approve», сейчас не enforced); конфликт интересов (как в метриках).

### Сквозные/технические (утв.)
- **Технический рефакторинг раздутых файлов** (без новых фич): `dashboards/service.py`
  (1031) → подмодули (данные виджетов / RLS / гранты / пресеты / подсказки / экспорт /
  шаблоны-версии); `DashboardsPage.tsx` (924) → вынести WidgetForm/SuggestPanel/
  SourceCatalog/AccessEditor/AlertEditor; при необходимости `api.ts` (589).
- **Этап Деплой** (автоматизация на реальном железе, [[feedback_deploy_automation]]):
  frontend-образ + reverse-proxy (nginx/Caddy: статика `dist/` + прокси API на один
  URL); `deploy.sh` (собрать→поднять→миграции→seed→smoke); авто-накат миграций;
  healthcheck api в compose; `.env.prod`+секреты+HTTPS; офлайн-поставка образов
  (docker save/load) под Astra; учесть уроки DS (NTP/часы, пин MinIO под CPU, amd64).

### Улучшения UX для обычного пользователя-зрителя (утв.)
- Отдельный чистый **read-only Viewer** (крупно, без конструкторских кнопок).
- **Поиск + избранное + недавние** на «Главной».
- **Расширенная карточка показателя:** значение + динамика + «что это значит» (текст
  модератора) + владелец + свежесть + цель + светофор план/факт.
- Тултипы у виджетов (единица/формула/период); уведомления по сработавшим алертам;
  мобильный адаптив / крупные шрифты для ТВ; онбординг-подсказки при первом входе.

### Профессиональные улучшения (утв., сквозные/позже)
- RLS на СТРОКИ данных (по отделу/региону), не только на дашборды.
- Кэш данных виджетов + батч-эндпоинт данных страницы.
- Планировщик обновления данных (авто-ingestion) + свежесть.
- Цели/бенчмарки, сравнение год-к-году, линейный тренд (без ИИ).
- Рассылка отчётов по расписанию (PDF на почту).
- Безопасность: MFA/парольные политики/блокировки, HTTPS в LAN.
- Наблюдаемость приложения (структурные логи, метрики; OpenTelemetry локально).
- Тесты в CI (API/e2e + GitHub Actions).
- Брендбук МФЦ / тёмная тема (ждём брендбук).

### Коннекторы/инфраструктура (пункт 10)
- **Для нашей разработки:** авторизовать **GitHub**-коннектор (PR/issues/CI-статусы,
  ревью). Остальные внешние SaaS (Datadog/Slack/Notion/Jira/Figma/…) НЕ подключать —
  система on-prem/LAN/госзаказчик (Astra), данные наружу нельзя.
- **Для прод-мониторинга:** НЕ коннекторы ко мне, а self-hosted в LAN —
  Prometheus + Grafana + node_exporter/cAdvisor + Loki (логи). Опц. Keycloak (SSO/MFA).
