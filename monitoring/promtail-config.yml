# Promtail — сбор логов контейнеров Docker и отправка в Loki.
# Использует docker service discovery (нужен доступ к docker.sock).
server:
  http_listen_port: 9080
  grpc_listen_port: 0
  log_level: warn

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 15s
    relabel_configs:
      # Имя контейнера → метка container.
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: container
      # Проект compose → метка project.
      - source_labels: ['__meta_docker_container_label_com_docker_compose_project']
        target_label: project
      # Сервис compose → метка service.
      - source_labels: ['__meta_docker_container_label_com_docker_compose_service']
        target_label: service
