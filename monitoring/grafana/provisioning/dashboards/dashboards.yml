# Grafana — провайдер дашбордов (загружает JSON из папки).
apiVersion: 1
providers:
  - name: Dashboard
    orgId: 1
    folder: Dashboard
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
